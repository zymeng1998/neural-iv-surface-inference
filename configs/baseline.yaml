# Baseline MLP training configuration — Phase 1

seed: 42
batch_size: 64
learning_rate: 0.001
epochs: 50
hidden_dim: 128

paths:
  processed_data_dir: data/processed
  checkpoint_dir: artifacts/checkpoints
  figures_dir: artifacts/figures
  tables_dir: artifacts/tables

# TODO: add model architecture and evaluation settings

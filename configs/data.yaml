# Data pipeline configuration — Phase 1

paths:
  raw_data_dir: data/raw
  interim_data_dir: data/interim
  processed_data_dir: data/processed
  sample_data_dir: data/samples

underlying: SPY
granularity: daily  # EOD only for Phase 1

# TODO: add date range, filtering, and normalization settings

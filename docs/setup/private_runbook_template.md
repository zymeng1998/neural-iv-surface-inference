# Private Runbook Template (Local Only)

## Important

This template is safe to keep in Git because it contains placeholders only.

Create a filled local-only copy at `docs/private/infra_local.md` and never commit that filled copy. The `.gitignore` rules in this repository will prevent it from being staged, but always verify before committing.

## Fields to Record Locally

- Remote provider:
- Pod / instance label:
- Public IP:
- External SSH port:
- SSH username:
- SSH host alias:
- Local SSH private key path:
- Local SSH config notes:
- GitHub auth notes:
- Cyberduck bookmark notes:
- Cursor Remote-SSH notes:
- Token / credential storage notes:
- Recovery / reconnect steps:
- Other operational details:

## Recommended Local Sections

### 1. Connection details

### 2. SSH configuration

### 3. GitHub authentication details

### 4. File transfer workflow

### 5. Editor / IDE workflow

### 6. Recovery checklist

### 7. Sensitive reminders

# Story 2A.3: Core uncertainty-evaluation metrics

---
id: 2A.3
epic: 2A
status: done   # backlog | todo | in_progress | in_review | blocked | done
created_at: 2026-05-22T01:29:47-04:00
last_updated_at: 2026-05-22T02:40:00-04:00
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`. Status changes happen in place; this file is never moved
> or deleted.

## Purpose

Implement the core, model-agnostic uncertainty-evaluation metrics that score any
predictor's reliability outputs. Roadmap W1 requires: prediction-interval
coverage (empirical vs nominal), interval width, error-vs-uncertainty
correlation, confidence-bucket metrics (error stratified by confidence), and
high-confidence MAE. These are pure functions over `(y_true, y_pred,
uncertainty/interval)` arrays — the measurement foundation every downstream
reliability claim depends on. Abstention curves are deferred to 2A.4.

## Context files to read first

- `docs/roadmaps/phase2_reliability_aware_surface_inference.md` (W1 bullets)
- `src/neural_iv_surface_inference/eval/predictor.py` (PredictionResult shape from 2A.2)
- `src/neural_iv_surface_inference/training/eval.py` (existing point-metric style to match)

## Files likely to touch

- `src/neural_iv_surface_inference/eval/uncertainty_metrics.py` (new)
- `tests/test_uncertainty_metrics.py` (new)

## Non-goals

- Do NOT implement abstention / risk–coverage curves — that is 2A.4.
- Do NOT build the end-to-end runner or write artifacts — that is 2A.5.
- Do NOT generate uncertainty signals; consume what `PredictionResult` carries.
- Do NOT change `training/eval.py`'s existing point metrics.

## Implementation steps

1. Implement pure functions, each taking numpy arrays and returning floats/dicts:
   - `interval_coverage(y_true, lower, upper) -> empirical coverage fraction`.
   - `mean_interval_width(lower, upper) -> float`.
   - `error_uncertainty_correlation(abs_error, uncertainty) -> {pearson, spearman}`.
   - `confidence_bucket_metrics(abs_error, confidence, n_buckets) -> per-bucket
     mean error + count` (error stratified by confidence quantile bucket).
   - `high_confidence_mae(abs_error, confidence, keep_fraction) -> MAE on the
     top-confidence retained subset`.
2. Handle NaN/empty inputs explicitly (return NaN with documented semantics);
   never silently drop.
3. Unit-test against synthetic data with analytically known answers:
   - Known-coverage Gaussian intervals → empirical coverage ≈ nominal.
   - Monotone error∝uncertainty → correlation ≈ 1.
   - Confidence buckets recover injected error ordering.

## Tests to run / add

- [ ] `pytest tests/test_uncertainty_metrics.py -q`
- [ ] `python3 scripts/smoke_test.py`

## Expected artifacts

- New module `src/neural_iv_surface_inference/eval/uncertainty_metrics.py`.
- New test file `tests/test_uncertainty_metrics.py`.

## Acceptance criteria

- [ ] All five metric families implemented as pure, typed functions.
- [ ] Synthetic-data tests confirm coverage tracks nominal within tolerance and
      correlation/bucket metrics recover injected structure.
- [ ] NaN/empty-input behavior is tested and documented.
- [ ] New tests pass; smoke test still passes.

## Rollback notes

Purely additive module + test. To undo: delete
`eval/uncertainty_metrics.py` and `tests/test_uncertainty_metrics.py`.
No existing code or data mutated.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal: Implement core uncertainty-evaluation metrics (2A.3).
- Mode: Implement (autonomous overnight run; human reviews in morning).
- Files inspected: eval/predictor.py (PredictionResult), training/eval.py
  (point-metric style), roadmap W1 bullets.
- Files changed: src/neural_iv_surface_inference/eval/uncertainty_metrics.py (new),
  tests/test_uncertainty_metrics.py (new).
- Tests run: pytest tests/test_uncertainty_metrics.py (26 passed); full suite
  tests/ (95 passed); scripts/smoke_test.py (exit 0).
- Results: five pure metric families implemented — interval_coverage,
  mean_interval_width, error_uncertainty_correlation (pearson+spearman),
  confidence_bucket_metrics, high_confidence_mae. NaN/empty rows dropped with
  documented returns; length mismatch raises ValueError. Synthetic tests
  confirm Gaussian coverage tracks nominal (~0.90), monotone error∝uncertainty
  gives corr≈1, buckets recover injected error ordering.
- Open questions: confidence convention (higher = more confident) is documented;
  callers convert raw uncertainty via confidence = -uncertainty. Confirm this
  matches what 2A.4/2A.5 expect when wiring the runner.
- Risks: pearson/spearman return nan on constant input (guarded) — by design.
- Recommended next task: 2A.4 (abstention / risk–coverage curves).
- Exact prompt for next Claude Code session: "Implement mode. Story 2A.4. Read
  docs/tasks/specs/2A.4_abstention_curves.md and only the context files it
  lists. Restate scope and acceptance criteria, propose a step plan, and wait
  for approval before editing."
```

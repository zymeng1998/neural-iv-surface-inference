# Story 3D.2 (Local): Phase 3 Results Notebook Generator Scaffold

---
id: 3D.2
epic: 3D
status: backlog   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-06-15T00:00:00-04:00
last_updated_at: 2026-06-15T00:00:00-04:00
parallel_safe_with: ["3D.3"]
file_scope:
  - "scripts/generate_phase3_results_notebook.py"
  - "tests/test_generate_phase3_results_notebook.py"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3D.2_phase3_notebook_generator.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`. Status changes happen in place; this file is
> never moved or deleted.
>
> `file_scope` is a **hard contract**.

## Purpose

Build the reproducible generator for the Phase 3 results notebook,
mirroring `scripts/generate_phase2_results_notebook.py` (2D.10). The
script is the single code surface Phase 3D is allowed to touch (per
[`3D.1`](3D.1_decompose_phase_3d.md) non-goals). It programmatically
assembles `notebooks/06_phase3_results.ipynb` from already-committed
Phase 3 bundles so the notebook is regenerable when artifacts change.
This story delivers the **script only**; 3D.4 runs it to emit the
committed `.ipynb`.

## Where this runs

**Local (Mac), CPU-only.** Pure pandas / matplotlib / nbformat over
committed artifacts. No model load, no Pod, no GPU, no data download.

## Scope contract (atomic story)

- **One question:** can the Phase 3 results notebook be reconstructed
  end-to-end from committed bundles by a single script (no manual cell
  authoring, no re-run of any model/eval)?
- **One artifact bundle:** `scripts/generate_phase3_results_notebook.py`
  (+ a small smoke test).
- **One acceptance check:** the script exits 0 and writes a notebook
  whose cells reference only committed paths; running it twice is
  idempotent (byte-stable section structure).

## Dependencies

- 3D.1 `done` (this decomposition; the spec + ADR 0009 skeleton exist).

## Context files to read first

- `scripts/generate_phase2_results_notebook.py` (the pattern to mirror)
- `scripts/generate_phase2c_results_notebook.py` (older precedent)
- `docs/tasks/specs/2D.10_phase2_results_memo_notebook.md`
- The committed Phase 3 bundles the notebook must render:
  - `results/3/spy_phase1_random40_noiselow/3a_compare/`,
    `3a_fourier/`, `3a_raw/`
  - `results/3/spy_phase1_random40_noiselow/3b_anp/`, `3b_compare/`
  - `results/3/spy_phase1_random40_noiselow_otm/3x_anp/`,
    `3x_compare/`, `rbf/`
  - `artifacts/runs/{3A,3B4,3B5,3C3,3X6,3X7,3X8,3X9,3X10,3X12}/`
    (manifests + `training_curve.csv`)
  - `results/2D/comparison_summary.csv` (the Phase 2D anchor)

## Files likely to touch

- `scripts/generate_phase3_results_notebook.py` (new).
- `tests/test_generate_phase3_results_notebook.py` (new — smoke: import +
  a dry build into a tmp path, assert 0 cell-build errors and that every
  referenced bundle path exists).

## Non-goals

- Do NOT write `docs/phase3_result_memo.md` (that is 3D.3).
- Do NOT commit the generated `notebooks/06_phase3_results.ipynb` here
  (that is 3D.4 — keep this story to the generator + test).
- Do NOT re-run any model, calibrator, eval, or comparison script; the
  notebook references committed artifacts by path only.
- Do NOT touch `src/` or any predictor / training code.

## Implementation steps

1. Copy the structure of `generate_phase2_results_notebook.py`.
2. Define the section list (mirrors the 3D.3 memo): TL;DR; the Phase 3
   ladder table (3A / 3B / 3X / 3C vs RBF vs Phase 2D); the dirty-vs-clean
   OTM restatement; per-epic figure pages (reference committed PNGs/CSVs
   by path); training-dynamics curves from the `artifacts/runs/3*`
   `training_curve.csv` files; the acceptance-criteria map; the
   open-questions / Phase 4 framing cell.
3. Read inputs with defensive path checks — fail loudly (non-zero exit)
   if any cited bundle path is missing, so a stale rename is caught.
4. Write the smoke test.
5. Update BOARD (3D.2 → `in_review`), PHASE3_INDEX, progress_log.
6. Run the PMR **live** gate (this commit touches `scripts/`, which is
   evidence-class).

## Tests to run / add

- [ ] `python3 scripts/generate_phase3_results_notebook.py --help` exits 0.
- [ ] `pytest tests/test_generate_phase3_results_notebook.py` green.
- [ ] Smoke build into a tmp path exits 0 with no cell-build errors.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose` (live — `scripts/`
      is evidence-class).

## Expected artifacts

- `scripts/generate_phase3_results_notebook.py`.
- `tests/test_generate_phase3_results_notebook.py`.
- Progress-log entry.

## Acceptance criteria

- [ ] The generator builds the notebook structure from committed bundles,
      failing loudly on any missing path.
- [ ] Smoke test green; running the generator twice is idempotent.
- [ ] No model / eval / data re-run; no `src/` change.
- [ ] PMR live gate passes.

## Rollback notes

Pure additions. `git checkout -- scripts/generate_phase3_results_notebook.py
tests/test_generate_phase3_results_notebook.py` (+ doc edits).

---

## Last checkpoint

### 2026-06-15T00:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of the 3D.1 decomposition.
- Last touched: this file.
- Next concrete action: gate on 3D.1 close, then implement the generator.
- Open blocker: 3D.1.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

# Story 3D.1: Decompose Phase 3D (Closing Memo + Re-Evaluation)

---
id: 3D.1
epic: 3D
status: todo
created_at: 2026-05-27T00:00:00-04:00
last_updated_at: 2026-06-14T23:10:00-04:00
parallel_safe_with: ["3A.1", "3B.1", "3C.1"]
file_scope:
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3D.1_decompose_phase_3d.md"
  - "docs/tasks/specs/3D.*_*.md"
  - "docs/roadmaps/phase3_accuracy_push.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/decisions/0009_*.md"
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`.

## Purpose

Phase-entry decomposition story for epic **3D — Phase 3 Closing Memo
+ Re-Evaluation** under workstream **W13**. Pure synthesis on the
committed `artifacts/runs/3*/` and `results/3/` bundles produced by
3A / 3B / 3C; no new training, no new eval runs.

Mirrors story 2D.10 (Phase 2 closing memo + notebook). Produces:

- `docs/phase3_result_memo.md` (headline table of all 3A / 3B / 3C
  variants versus RBF and versus the Phase 2D close).
- `notebooks/06_phase3_results.ipynb` (regenerable from
  `scripts/generate_phase3_results_notebook.py`).
- Closing entry in `docs/experiments/experiment_journal.md`.
- **ADR 0009 — Phase 3 production predictor selection**, which locks
  the variant that becomes the new production surface inference
  predictor (or documents the absence of one if the bar was missed).
  **(Renumber note: this story originally said "ADR 0008"; that number
  is now taken by the Phase 3C microstructure-feature-set freeze
  ([ADR 0008](../../decisions/0008_microstructure_feature_set_freeze.md),
  Implemented 2026-06-14). The production-selection ADR is therefore
  **0009**.)**
  Given that all four Phase 3 gating epics (3A architecture, 3X data
  correction, 3C features) missed the ≥ 5 %-below-RBF bar on the clean
  OTM substrate, ADR 0009 is expected to record **no pure
  conditional-neural production predictor** and to lock the forward
  direction as **Phase 4 = RBF-prior hybrid / residual neural model**
  (RBF carries the local interpolation it already wins; the neural model
  learns the residual + the calibrated reliability / abstention layer).
  See roadmap §W13.

## Context files to read first

- `docs/decisions/0004_phase3_accuracy_push_framing.md`
- `docs/roadmaps/phase3_accuracy_push.md` (§5 acceptance criteria)
- `docs/phase2_result_memo.md` and `scripts/generate_phase2_results_notebook.py`
  (precedent for the synthesis story)
- `docs/tasks/specs/2D.10_phase2_results_memo_notebook.md`
  (decomposition precedent)
- `docs/tasks/_template.md`
- `docs/PHASE3_INDEX.md`
- All committed `results/3/<dataset>/<variant>/metrics_summary.csv`
  bundles
- All committed `artifacts/runs/3*/` manifests + training curves +
  predictions
- 3A / 3B / 3C closing entries in
  `docs/experiments/experiment_journal.md`

## Files likely to touch

- `docs/tasks/BOARD.md`
- `docs/tasks/specs/3D.*_*.md`
- `docs/roadmaps/phase3_accuracy_push.md` (§ W13 concrete story list)
- `docs/PHASE3_INDEX.md`
- `docs/logs/progress_log.md`
- `docs/decisions/0009_phase3_production_predictor_selection.md`

## Non-goals

- Do NOT run any training, any eval, or any data download.
- Do NOT touch source code outside
  `scripts/generate_phase3_results_notebook.py` (the synthesis script
  is the only code surface 3D may touch).
- Do NOT extend Phase 3's scope — if synthesis reveals a needed
  experiment, capture it as a new follow-up backlog story (epic 3E or
  a Phase 4 placeholder), do not absorb into 3D.
- Do NOT close epic 3D before all of 3A, 3B, 3C are `done`.

## Implementation steps

1. Read all closing entries in
   `docs/experiments/experiment_journal.md` for 3A / 3B / 3C.
2. Verify that every Phase 3 variant has a committed
   `artifacts/runs/3*/<variant>/manifest.json` + curves + predictions
   and a `results/3/<dataset>/<variant>/` bundle. If any are missing,
   stop and escalate.
3. Set epic **3D** to `in_progress` on `BOARD.md`.
4. Draft atomic 3D stories. Skeleton:
   - **3D.2** local: `scripts/generate_phase3_results_notebook.py`
     scaffold (parallels `generate_phase2_results_notebook.py`).
   - **3D.3** local: `docs/phase3_result_memo.md` with the headline
     acceptance-criteria map and the variant comparison table.
   - **3D.4** local: regenerated `notebooks/06_phase3_results.ipynb`,
     **ADR 0009** (production predictor selection — frames Phase 4 =
     RBF-prior hybrid / residual neural model), and the closing
     `experiment_journal.md` entry. May also create a **Phase 4 epic
     placeholder** (backlog) on the BOARD if the closing memo warrants
     it — do not decompose Phase 4 here.
5. Fully fill each new story spec using `_template.md`.
6. Update Phase 3 roadmap § W13.
7. Update `docs/PHASE3_INDEX.md`.
8. Append `docs/logs/progress_log.md`.
9. Run PMR gate dry-run.
10. Mark `in_review`.

## Tests to run / add

- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- This spec.
- New `docs/tasks/specs/3D.*_*.md`.
- Updated `docs/tasks/BOARD.md`, roadmap, index, progress log.

## Acceptance criteria

- [ ] Epic 3D is `in_progress` on `BOARD.md`.
- [ ] All of 3A, 3B, 3C are `done` before this story moves to
      `in_progress`. (Spec may be drafted in advance, but execution
      gates on prior epics.)
- [ ] Each new 3D.* story has a fully-populated spec.
- [ ] Each new 3D.* story has a `backlog` row on `BOARD.md`.
- [ ] Phase 3 roadmap § W13 reflects the concrete story list.
- [ ] `docs/PHASE3_INDEX.md` reflects the new stories.
- [ ] PMR gate dry-run passes.
- [ ] No source code, config, data, model, or training artifact
      modified.

## Rollback notes

Docs-only. `git checkout -- docs/`.

---

## Last checkpoint

### 2026-05-27T00:00:00-04:00 — initial scaffold (not yet executed)

- Last completed: spec written by the Phase 3 scaffolding pass.
- Last touched: this file.
- Next concrete action: gate this story on 3A / 3B / 3C closing.
- Open blocker: depends on all three sibling epics closing.

### 2026-06-14T23:10:00-04:00 — readied; promoted `backlog → todo`

- All gating epics (3A / 3B / 3X / 3C) are `done`, so the gate is
  cleared and this story is safe to pick up cold.
- Spec readied: fixed the ADR-number collision (production-selection ADR
  is **0009**, not 0008 — 0008 is the microstructure freeze) in the
  Purpose, `file_scope`, "Files likely to touch", and the 3D.4 skeleton;
  added the **Phase 4 = RBF-prior hybrid / residual neural model**
  framing the closing memo / ADR 0009 must carry.
- Not executed: this is preparation only. Running 3D.1 will set epic 3D
  `in_progress`, draft the 3D.2 / 3D.3 / 3D.4 child specs, and update
  roadmap §W13 + the index with the concrete story list.
- Next concrete action: operator/session executes the 3D.1 decomposition.
- Open blocker: none.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

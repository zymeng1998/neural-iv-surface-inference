# Story 4A.2 (Local): Residual-Target Builder + `target_mode` Loader Flag

---
id: 4A.2
epic: 4A
status: backlog   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-06-18T00:00:00-04:00
last_updated_at: 2026-06-18T00:00:00-04:00
parallel_safe_with: []
file_scope:
  - "src/neural_iv_surface_inference/data/conditional_loaders.py"
  - "src/neural_iv_surface_inference/data/residual_targets.py"
  - "scripts/build_residual_targets.py"
  - "tests/test_residual_targets.py"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/4A.2_residual_target_builder.md"
  - "docs/PHASE4_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/data/data_lineage.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a hard
> contract.

## Purpose

Build the only genuinely new code surface in Phase 4: compute the per-date
RBF predictions at **query** coordinates and the additive residual target
`r = iv_true − rbf_pred` (ADR 0010), and expose a
`target_mode ∈ {absolute, residual}` flag on the conditional loader so the
existing trainer can fit `f_θ` on residuals with no other change. Ships the
builder + flag + tests **only** — the full dataset build is 4A.3, training
is 4A.4.

## Where this runs

**Local (Mac), CPU-only.** Smoke on a small date slice; no full build.

## Scope contract (atomic story)

- **One question:** can the loader yield residual targets (`iv − rbf_pred`)
  behind a flag, with the RBF-at-query computation matching the 3X.6 RBF
  baseline recipe, and `absolute` mode bit-identical to today?
- **One artifact bundle:** `residual_targets.py` (RBF-at-query + residual
  computation), `build_residual_targets.py` (driver), the `target_mode`
  flag, and tests.
- **One acceptance check:** `absolute` mode reproduces current loader output
  byte-for-byte; `residual` mode yields `iv − rbf_pred` with finite values
  on a smoke slice; RBF-at-query parity vs the 3X.6 baseline within tol.

## Dependencies

- 4A.1 `done` (this decomposition + ADR 0010 on `main`).

## Context files to read first

- `src/neural_iv_surface_inference/data/conditional_loaders.py` (the loader
  to extend; `target_mode` mirrors the 3C.2 `feature_set` flag pattern).
- The 3X.6 RBF baseline implementation (per-date RBF; reuse, do not
  re-derive) — see `docs/tasks/specs/3X.6_remote_rbf_on_otm_baseline.md`.
- `docs/data/data_lineage.md` (record the new derived residual column/flow).

## Files likely to touch

- `src/.../data/residual_targets.py` (new): `rbf_predict_at_queries(...)`
  reusing the baseline RBF, `residual_targets(...)` returning `iv − rbf`.
- `src/.../data/conditional_loaders.py`: add `target_mode` (default
  `absolute`, byte-identical legacy path).
- `scripts/build_residual_targets.py` (new): driver to materialise residuals
  for a dataset (used at scale by 4A.3).
- `tests/test_residual_targets.py` (new).

## Non-goals

- Do NOT build the full OTM residual dataset (that is 4A.3) — smoke slice
  only.
- Do NOT train anything (4A.4). Do NOT change the RBF baseline math.
- Do NOT change `absolute`-mode behaviour (every committed checkpoint must
  stay reproducible).

## Implementation steps

1. Factor the per-date RBF predict into `residual_targets.py` reusing the
   3X.6 recipe; add `residual_targets()`.
2. Add `target_mode` to the loader; `residual` subtracts `rbf_pred` from the
   IV target at query coords.
3. Driver `build_residual_targets.py` (writes residual parquet alongside the
   conditional dataset; full run deferred to 4A.3).
4. Tests: `absolute` parity (byte-identical), `residual` finiteness + a
   hand-checked tiny case, RBF-at-query parity vs baseline within tol.
5. Update `data_lineage.md`; PMR live gate.

## Tests to run / add

- [ ] `absolute`-mode loader output unchanged vs current (regression).
- [ ] `residual`-mode = `iv − rbf_pred` on a smoke slice; all finite.
- [ ] RBF-at-query parity vs the 3X.6 baseline within tolerance.
- [ ] `pytest tests/test_residual_targets.py`
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose` (live — `src/` touched).

## Expected artifacts

- `residual_targets.py`, `build_residual_targets.py`, `target_mode` flag,
  `tests/test_residual_targets.py`, lineage update.

## Acceptance criteria

- [ ] `residual` mode yields finite `iv − rbf_pred`; `absolute` unchanged.
- [ ] Tests green; PMR live gate passes.
- [ ] No full build, no training, no GPU.

## Compute requirements

- **Locale:** local Mac
- **Hardware class:** CPU-only
- **Approx. VRAM:** n/a
- **Approx. wall time:** ~30 min smoke + test
- **Disk — local committed artifacts:** ~10 kB code/tests (no data committed)
- **Disk — remote working set:** n/a
- **Notes:** RBF-at-query on a small slice only; full materialisation is
  4A.3.

## Rollback notes

`git checkout -- src/ scripts/ tests/ docs/` — additive; `absolute` default
preserves all prior behaviour.

---

## Last checkpoint

### 2026-06-18T00:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of the 4A.1 decomposition.
- Next concrete action: gate on 4A.1 close; then implement the builder.
- Open blocker: 4A.1.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

# Story 3C.8 (Local): 3C Closing Addendum + ADR 0008 → Implemented

---
id: 3C.8
epic: 3C
status: in_review
created_at: 2026-06-02T22:00:00-04:00
last_updated_at: 2026-06-02T22:00:00-04:00
parallel_safe_with: ["3D.1"]
file_scope:
  - "docs/roadmaps/phase3_accuracy_push.md"
  - "docs/decisions/0008_microstructure_feature_set_freeze.md"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3C.8_local_3c_closing_addendum.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
  - "README.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a
> hard contract.

## Purpose

Close epic 3C. Synthesises 3C.6 (decision-layer eval) + 3C.7 (comparison
tables) into the §W12 closing addendum, flips ADR 0008 to
**Implemented** with its Outcome block filled, journals the close-out,
and propagates the status change to README. **Pure synthesis on
committed artifacts; no model, baseline, or pipeline run.**

Mirrors 3X.14 in shape; differs in scope (Q3 — full Phase 3 closing
memo remains a 3D deliverable, not 3C.8).

## Where this runs

**Local (Mac), CPU-only.** Reads committed bundles + the §W11 / §W11.5
closing addenda. Writes only docs.

## Scope contract (atomic story)

- **One question:** with the `micro_v1` feature set frozen by ADR
  0008 and trained on the matched OTM substrate, what is the verdict
  on the Phase 3 acceptance bar (≥ 5 % below RBF on test MAE, coverage
  within ±2 pp of 0.90, hi-conf MAE strictly below no-abstention,
  forbidden-flag violations not meaningfully greater than 3X.12),
  and what is the recommended forward path (3D close / 3C extension
  to `micro_v2` / Phase 4 RBF-prior hybrid)?
- **One artifact bundle:** the §W12 closing addendum, ADR 0008's
  Outcome block, the journal close-out entry.
- **One acceptance check:** every numeric claim in the addendum cites
  a 3C.6 / 3C.7 / 3X.* committed bundle path; the no-overclaim
  guardrail is preserved (scoped to `random40_noiselow_otm` only); a
  forward recommendation is recorded.

## Dependencies

- 3C.6 `done` (decision-layer numbers committed).
- 3C.7 `done` (comparison tables committed).
- 3X.14 `done` (its §W11.5 close-out is the template).

## Context files to read first

- `docs/roadmaps/phase3_accuracy_push.md` §W11 (3B close), §W11.5 (3X
  close), §W12 (current 3C placeholder)
- [ADR 0008](../../decisions/0008_microstructure_feature_set_freeze.md)
  (the Outcome block to fill)
- `results/3/spy_phase1_random40_noiselow_otm/3c_compare/comparison_wide.csv`
  and `headline.md` (the 3C.7 evidence base)
- `results/3/spy_phase1_random40_noiselow_otm/3c_anp_micro/metrics_summary.csv`
  (the 3C.6 calibrated production numbers)
- `docs/tasks/specs/3X.14_local_closing_addendum.md` (the closest
  precedent in shape — verdict-against-bar + recommendation
  + no-overclaim guardrail)
- `docs/research/duplicate_coordinate_methodology_progression.md` (the
  3X narrative this story's verdict block stitches onto)

## Files likely to touch

- `docs/roadmaps/phase3_accuracy_push.md` — replace the §W12 placeholder
  with a "3C closing addendum" block: headline test MAE per family×head
  (cited from 3C.7 wide table), calibrated production verdict against
  the bar (cited from 3C.6), Phase 4 recommendation. Also update the
  Phase 3 status block at the file's tail.
- `docs/decisions/0008_microstructure_feature_set_freeze.md` — flip
  status to `Implemented (YYYY-MM-DD)`; fill the Outcome block with
  the 3C.6 / 3C.7 numbers; answer the three "open questions" the ADR
  deferred to 3C.8.
- `docs/tasks/BOARD.md` — flip 3C.8 row to `in_review` (then `done`
  on operator approval); flip epic 3C row to `done` with a one-line
  verdict summary; update timestamps.
- `docs/PHASE3_INDEX.md` — sync the 3C rows; update the 30-second
  orientation block; add 3C.8 to the "Per-story last checkpoint"
  block; flip the Phase 3 "next action" pointer (3D.1 promotion if
  3C closes ≥ partial; otherwise Phase 4 hybrid kickoff).
- `docs/logs/progress_log.md` — append entry mirroring the 3X.14
  log block (files updated, non-goals respected, tests, next actions).
- `docs/experiments/experiment_journal.md` — close-out entry with
  the headline numbers and verdict-against-bar (similar to 3X.14's
  close-out journal entry).
- `README.md` — flip 3C from "NEXT" to either `done` (if Phase 3 bar
  met) or `partial` / `paused → 3D / Phase 4` (otherwise); update
  the Phase 3 status callout.

## Non-goals

- Do NOT write the full Phase 3 closing memo
  (`docs/phase3_result_memo.md`) — that is 3D's deliverable (Q3
  carried forward from 3X).
- Do NOT touch the personal notebooks
  (`docs/neural_iv_surface_*_personal_notes.md`,
  `docs/neural_iv_surface_research_methodology_narrative.md`) —
  gitignored, operator-only.
- Do NOT re-run any model, calibrator, eval, or comparison script.
  Every number must already be in a committed bundle.
- Do NOT make any robustness claim across the other 10 OTM benchmark
  variants — the all-11 retraining study is still deferred (3X
  no-overclaim guardrail carries forward).
- Do NOT touch any source code, config, or training artifact.
- Do NOT pre-empt 3D's decomposition (only flag if 3C result changes
  3D scope).

## Compute requirements

- **Locale:** local Mac
- **Hardware class:** CPU-only
- **Approx. VRAM:** n/a
- **Approx. wall time:** ~2 h (docs synthesis)
- **Disk — local committed artifacts:** ~5 kB net (text edits)
- **Disk — remote working set:** n/a
- **Notes:** entirely docs.

## Implementation steps

1. Read 3C.6 metrics + 3C.7 comparison tables; extract the headline
   numbers.
2. Draft the §W12 closing addendum block. Include:
   - headline test MAE per family×head for `micro_v1` vs OTM baseline;
   - calibrated production test MAE / hi-conf / coverage / band width
     / abstention / flag violations vs 3X.12;
   - delta-vs-RBF-on-OTM (the 0.00613 floor);
   - verdict against the Phase 3 bar (met / partial / NOT met);
   - forward recommendation (3D close / `micro_v2` / Phase 4 hybrid);
   - no-overclaim guardrail (scoped to matched substrate only).
3. Fill ADR 0008 Outcome block; flip status to Implemented; answer
   the three deferred open questions if the evidence allows
   (otherwise restate as carried forward).
4. Update the Phase 3 status block at the roadmap file's tail.
5. Update BOARD: 3C.8 → `in_review`; epic 3C → `done` with one-line
   verdict; epic 3D row's "Updated" timestamp refresh.
6. Update PHASE3_INDEX (Live status, 30-second orientation, Per-story
   last checkpoint).
7. Append progress_log entry.
8. Append journal close-out entry.
9. Update README's Phase 3 status callout.
10. PMR gate dry-run.
11. Flip BOARD row to `in_review`.

## Tests to run / add

- [ ] Manual: every numeric claim in the §W12 addendum has a `bundle_path`
      citation pointing at a committed artifact (provenance audit).
- [ ] No-overclaim audit: no sentence claims robustness across the
      other 10 OTM variants.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- §W12 closing addendum in `docs/roadmaps/phase3_accuracy_push.md`.
- ADR 0008 status → Implemented; Outcome block filled.
- BOARD / INDEX updates.
- progress_log + journal entries.
- README update.

## Acceptance criteria

- [ ] §W12 addendum has explicit verdict against the Phase 3 bar.
- [ ] Every numeric claim cites a committed-bundle path.
- [ ] No-overclaim guardrail preserved (matched substrate only).
- [ ] ADR 0008 → Implemented with Outcome block filled.
- [ ] BOARD, PHASE3_INDEX, README in sync with the new status.
- [ ] PMR gate dry-run passes.
- [ ] No source code or training artifact modified.

## Rollback notes

Docs-only. `git checkout -- docs/ README.md`.

---

## Last checkpoint

### 2026-06-02T22:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of 3C.1 decomposition.
- Last touched: this file.
- Next concrete action: gate on 3C.7 close.
- Open blocker: 3C.7.

### 2026-06-14T00:00:00-04:00 — executed (in_review), adapted close

- **Scope deviation from the spec, operator-directed.** The spec assumed
  3C.6 (decision-layer) + 3C.7 (comparison) numbers as inputs. 3C.3
  returned a clean negative — `micro_v1` worsened test MAE in all three
  heads vs the 3X.9 minimal baseline (gauss +0.00194 / quant +0.00187 /
  point +0.00452; all above the RBF floor 0.00613) — which settles the
  headline 3C question against `micro_v1`. The operator directed that
  the downstream eval chain **3C.4–3C.7 be cancelled** (no longer
  informative on a known-worse feature set) and 3C close on 3C.3's
  training evidence alone. Every numeric claim in the §W12 addendum
  therefore cites 3C.3 (`artifacts/runs/3C3/**` + journal) and the 3X.6
  / 3X.9 OTM baselines — no 3C.6 / 3C.7 citations exist because those
  bundles were never produced. The no-overclaim guardrail (matched
  `random40_noiselow_otm` substrate only) is preserved.
- Files changed (this close): `docs/roadmaps/phase3_accuracy_push.md`
  (§W12 closing addendum + 3C.4–3C.7 marked cancelled + §W13 Phase 4
  framing + status block + §W11.5 3X.7/3X.8 drift fix), ADR 0008 →
  Implemented (Outcome block filled, three deferred questions answered),
  `docs/tasks/BOARD.md`, `docs/PHASE3_INDEX.md`, `docs/logs/progress_log.md`,
  `docs/experiments/experiment_journal.md`, `README.md`, the 3C.4–3C.7
  specs (→ cancelled with rationale), the 3B.1 / 3B.6 / 3B.7 specs
  (in_review → done; 3B dirty-substrate verdict marked superseded by 3X),
  and `STATUS.md`. Pure docs/planning; no model / eval / pipeline /
  source-code run.
- Verdict against the Phase 3 bar: **NOT met.** Forward: 3D close +
  Phase 4 RBF-prior hybrid / residual neural model.
- Next concrete action: operator promotes 3C.8 → `done`; then promotes
  3D.1 (`backlog → todo`).
- Open blocker: none.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

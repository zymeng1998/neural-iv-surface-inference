# Story 3X.5 (Remote, CPU): Audit OTM strict + all 11 benchmarks — HUMAN REVIEW GATE

---
id: 3X.5
epic: 3X
status: backlog   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-05-29T23:55:00-04:00
last_updated_at: 2026-05-29T23:55:00-04:00
parallel_safe_with: ["3C.1", "3D.1"]
file_scope:
  - "artifacts/audits/duplicate_coordinates_otm/**"
  - "docs/research/duplicate_coordinate_audit_otm.md"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3X.5_remote_audit_otm_gate.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. This story is the
> **gate**: GPU retraining (3X.7+) must not begin until its acceptance
> criteria pass and a human reviews the result.

## Purpose

Run audit v2 (from 3X.3) against the OTM strict surface + all 11 OTM
benchmarks (from 3X.4) to **prove the correction worked**. This is the
explicit human-review checkpoint in the split rental (Q4): if the OTM
audit passes, the CPU pod's work is done and a GPU pod can be rented; if
it fails, paired-coordinate masking (D4) or another correction becomes
mandatory before any OTM benchmark is used for claims.

## Where this runs

**Remote (RunPod), CPU pod** — same pod as 3X.4 (data is already there).
12 audit passes (1 strict + 11 benchmarks). Outputs pulled back and
committed (the per-cell CSVs are small).

## Scope contract (atomic story)

- **One question:** does the OTM substrate clear the ADR 0006 audit
  gate — contract-key dup ≤ 0.5 %, coord-key dup ≤ 0.5 % @ 10 dp, and
  exact-twin observed/held-out leakage ≤ 0.5 % per split — for the
  strict file and **every** one of the 11 benchmarks?
- **One artifact bundle:** `artifacts/audits/duplicate_coordinates_otm/`
  (per-artifact summary CSVs + a roll-up `otm_audit_gate.csv` with
  pass/fail per artifact) + `docs/research/duplicate_coordinate_audit_otm.md`.
- **One acceptance check:** the roll-up shows PASS for all 12 artifacts,
  OR a clearly-documented FAIL that routes to the D4 paired-masking
  remediation (which re-runs 3X.4 benchmark build with `paired_coord=True`
  and re-audits).

## Dependencies

- 3X.3 `done` (audit v2 exists, parity-tested).
- 3X.4 `done` (OTM strict + 11 benchmarks on the Pod volume).

## Context files to read first

- `scripts/audit_duplicate_coordinates_v2.py`
- `docs/research/duplicate_coordinate_audit.md` (the dirty baseline to
  contrast against)
- `docs/decisions/0006_duplicate_coordinate_data_correction.md` (the
  gate thresholds + D4 trigger)

## Files likely to touch

- `artifacts/audits/duplicate_coordinates_otm/*.csv` (per-artifact +
  roll-up; small, committed)
- `docs/research/duplicate_coordinate_audit_otm.md` (auto-generated
  roll-up report)
- BOARD / PHASE3_INDEX / progress log / experiment journal

## Non-goals

- Do NOT train anything (the gate must pass first).
- Do NOT rent a GPU pod for this story (CPU only).
- Do NOT silently proceed past a FAIL — a FAIL routes to D4 remediation
  and a fresh human review.
- Do NOT commit the large per-row CSVs if any exceed a few MB (commit
  the compact summaries only, per the audit-design convention).

## Compute requirements

- **Locale:** remote (RunPod)
- **Hardware class:** **CPU-only**
- **Approx. VRAM:** n/a
- **Approx. wall time:** with audit v2, ~10–15 min for all 12 artifacts
  (vs ~2.6 h for v1 on a single artifact — the reason v2 exists)
- **Disk — local committed artifacts:** ~12 small summary CSVs + roll-up
  + markdown (< 2 MB total)
- **Disk — remote working set:** reuses 3X.4 data (no new large files)
- **Notes:** last CPU-pod story before the review gate. After this
  passes and the human approves, **terminate the CPU pod** (Q4) — but
  first re-verify all OTM data + audit artifacts are on persistent
  storage, since the GPU stories re-attach the same volume.

## Implementation steps

1. Read the context files; confirm 3X.3 + 3X.4 are `done`.
2. Run audit v2 on the OTM strict file → `..._otm/strict/`.
3. Loop audit v2 over all 11 OTM benchmarks → `..._otm/<variant>/`.
4. Build the roll-up `otm_audit_gate.csv`: one row per artifact with
   the three dup/leakage shares + a PASS/FAIL verdict against the 0.5 %
   thresholds.
5. Generate `docs/research/duplicate_coordinate_audit_otm.md` with the
   dirty-vs-OTM contrast headline (e.g. 93.61 % → expected ~0 %).
6. **If any artifact FAILS:** stop, document, and open the D4
   paired-masking remediation path (re-run 3X.4 build with
   `paired_coord=True`, re-audit). Do not proceed to GPU stories.
7. Pull back summaries; commit; journal + index + log.
8. PMR gate dry-run; flip BOARD to `in_review`; **request human review
   of the gate** before 3X.6/3X.7 proceed.

## Tests to run / add

- [ ] audit v2 completes on all 12 artifacts.
- [ ] roll-up `otm_audit_gate.csv` has a verdict for every artifact.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- `artifacts/audits/duplicate_coordinates_otm/strict/*.csv`
- `artifacts/audits/duplicate_coordinates_otm/<variant>/*.csv` (×11)
- `artifacts/audits/duplicate_coordinates_otm/otm_audit_gate.csv` (roll-up)
- `docs/research/duplicate_coordinate_audit_otm.md`

## Acceptance criteria

- [ ] All 12 artifacts audited; roll-up complete.
- [ ] **GATE:** every artifact PASSES (contract-key dup ≤ 0.5 %,
      coord-key dup ≤ 0.5 % @ 10 dp, exact-twin leakage ≤ 0.5 % per
      split) — OR a FAIL is documented with the D4 remediation opened.
- [ ] Dirty-vs-OTM contrast recorded in the report (audit headline
      drops from 93.61 % to the OTM value).
- [ ] PMR gate dry-run passes.
- [ ] Human review of the gate requested before any GPU story.

## Rollback notes

`git rm -r artifacts/audits/duplicate_coordinates_otm/ docs/research/duplicate_coordinate_audit_otm.md`.
No data mutated.

---

## Last checkpoint

### 2026-05-29T23:55:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of 3X.1 decomposition.
- Last touched: this file.
- Next concrete action: 3X.4 closes `done`; human promotes 3X.5 →
  `todo`; same CPU pod runs the 12-artifact audit.
- Open blocker: 3X.4 must be `done`.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

# Story 3X.6 (Remote, CPU): Early RBF-on-OTM baseline

---
id: 3X.6
epic: 3X
status: backlog   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-05-29T23:55:00-04:00
last_updated_at: 2026-05-29T23:55:00-04:00
parallel_safe_with: ["3C.1", "3D.1"]
file_scope:
  - "artifacts/runs/3X6/**"
  - "results/3/spy_phase1_random40_noiselow_otm/rbf/**"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3X.6_remote_rbf_on_otm_baseline.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`.

## Purpose

Establish the **RBF floor** on the clean `random40_noiselow_otm`
benchmark *early* — before any GPU spend — as a sanity check that the
OTM benchmark construction produced a sensibly-difficult problem
(operator's "RBF-on-OTM early checkpoint" instruction). RBF is the
per-date interpolation baseline the whole Phase 3 bar is defined
against; knowing its OTM scale de-risks the GPU retraining that follows.

## Where this runs

**Remote (RunPod), CPU pod** — RBF is `scipy.interpolate`, no GPU
needed; runs on the same CPU pod as 3X.4 / 3X.5 right after the audit
gate passes. Outputs committed under `results/3/..._otm/rbf/`.

## Scope contract (atomic story)

- **One question:** what is RBF-on-OTM test/val MAE on
  `random40_noiselow_otm`, and how does it compare to RBF-on-dirty
  (0.0730 slice / 0.0662 full fold)?
- **One artifact bundle:** `artifacts/runs/3X6/rbf_otm/predictions_{val,test}.parquet`
  + `results/3/spy_phase1_random40_noiselow_otm/rbf/metrics_summary.csv`.
- **One acceptance check:** RBF-on-OTM produces finite predictions on
  every date; MAE is in a plausible range (flag if wildly off — that
  would indicate an OTM-build problem feeding back to 3X.4).

## Dependencies

- 3X.5 `done` and **audit gate PASSED** (human-reviewed).
- `random40_noiselow_otm` benchmark on the Pod volume.

## Context files to read first

- `src/neural_iv_surface_inference/models/interpolation.py`
- `docs/tasks/specs/3B.7_local_decision_layer_eval.md` (RBF-on-dirty
  numbers to contrast)
- `results/3/spy_phase1_random40_noiselow/3b_compare/comparison.csv`

## Files likely to touch

- `artifacts/runs/3X6/rbf_otm/predictions_{val,test}.parquet` (committed
  or gitignored per size; metrics CSV always committed)
- `results/3/spy_phase1_random40_noiselow_otm/rbf/metrics_summary.csv`
- BOARD / PHASE3_INDEX / progress log / experiment journal

## Non-goals

- Do NOT train any neural model (that is 3X.7+).
- Do NOT use RBF as a prior / residual base for any neural model (per
  ADR 0004 — RBF stays an independent baseline).
- Do NOT run on any benchmark other than `random40_noiselow_otm`.
- Do NOT rent a GPU pod for this (CPU only).

## Compute requirements

- **Locale:** remote (RunPod)
- **Hardware class:** **CPU-only**
- **Approx. VRAM:** n/a
- **Approx. wall time:** ~20–40 min (per-date RBF over the full fold,
  matched to the dirty RBF run)
- **Disk — local committed artifacts:** metrics CSV (<1 MB); predictions
  parquet ~40–80 MB (gitignore if large, per the 2D.9 convention)
- **Disk — remote working set:** reuses 3X.4 benchmark (no new large data)
- **Notes:** this is the **last CPU-pod story**. If the operator wants
  to minimise rental windows, run 3X.4 → 3X.5 → 3X.6 in one CPU session,
  then review the gate + RBF scale, then rent the GPU pod for 3X.7+.

## Implementation steps

1. Confirm 3X.5 gate PASSED.
2. Run the RBF interpolation baseline on `random40_noiselow_otm`
   (val + test), mirroring the dirty RBF protocol exactly (same method,
   same kernel, same per-date handling).
3. Write `metrics_summary.csv` (val/test MAE, plus the standard
   moneyness/maturity-bucketed MAE if cheap).
4. Contrast against RBF-on-dirty in the journal entry; flag if the OTM
   MAE is implausible (feeds back to 3X.4 if so).
5. Pull back; commit; journal + index + log.
6. PMR gate dry-run; flip BOARD to `in_review`.

## Tests to run / add

- [ ] RBF-on-OTM produces finite predictions on every date.
- [ ] `metrics_summary.csv` has val + test MAE.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- `artifacts/runs/3X6/rbf_otm/predictions_{val,test}.parquet`
- `results/3/spy_phase1_random40_noiselow_otm/rbf/metrics_summary.csv`
- journal entry with the RBF dirty-vs-OTM contrast.

## Acceptance criteria

- [ ] Finite RBF-on-OTM predictions on all dates.
- [ ] val + test MAE recorded and contrasted with RBF-on-dirty.
- [ ] No neural training performed.
- [ ] PMR gate dry-run passes.

## Rollback notes

`git rm -r artifacts/runs/3X6/ results/3/spy_phase1_random40_noiselow_otm/rbf/`.
No data mutated.

---

## Last checkpoint

### 2026-05-29T23:55:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of 3X.1 decomposition.
- Last touched: this file.
- Next concrete action: 3X.5 gate passes + human review; run RBF-on-OTM
  on the same CPU pod.
- Open blocker: 3X.5 audit gate must PASS.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

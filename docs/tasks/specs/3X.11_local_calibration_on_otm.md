# Story 3X.11 (Local): Calibrator re-fit on OTM val predictions

---
id: 3X.11
epic: 3X
status: in_review   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-05-29T23:55:00-04:00
last_updated_at: 2026-06-02T00:30:00-04:00
parallel_safe_with: ["3C.1", "3D.1"]
file_scope:
  - "configs/calibration_3X11_anp_otm.yaml"
  - "artifacts/calibration/3X11_anp_otm.json"
  - "tests/test_calibration_anp_otm.py"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3X.11_local_calibration_on_otm.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. The calibrator JSON
> is gitignored (regenerable); the config + test + manifest are committed.

## Purpose

Re-fit the 2D.4-style fused calibrator (temperature + ensemble-scale)
on the **OTM** ANP val predictions (3X.9 gaussian/quantile + 3X.10
ensemble), mirroring 3B.6 on the clean substrate. Produces the
calibrator the OTM decision-layer eval (3X.12) consumes.

## Where this runs

**Local Mac, CPU** — fast fit on pulled-back val predictions, no GPU,
mirroring 3B.6 (which ran locally; the Pod-side regeneration in 3B.7 was
bit-identical).

## Scope contract (atomic story)

- **One question:** what temperature `T`, ensemble-scale, and masking
  fusion fit the OTM ANP val predictions, and does OTM val coverage @0.90
  land within ±2 pp with hi-conf MAE < no-abstention MAE?
- **One artifact bundle:** `configs/calibration_3X11_anp_otm.yaml` +
  `artifacts/calibration/3X11_anp_otm.json` (gitignored) + a committed
  fit-manifest summary.
- **One acceptance check:** fit converges; val coverage @0.90 within
  ±2 pp; val hi-conf MAE < no-abstention MAE; parity test green.

## Dependencies

- 3X.9 done (OTM ANP gaussian/quantile val predictions).
- 3X.10 done (OTM ANP ensemble val predictions).

## Context files to read first

- `configs/calibration_3B6_anp.yaml`
- `scripts/run_calibration_fit.py`
- `docs/tasks/specs/3B.6_local_calibrator_refit.md`
- `tests/test_calibration_anp.py`

## Files likely to touch

- `configs/calibration_3X11_anp_otm.yaml` (clone of 3B.6, inputs
  re-pointed at 3X.9 / 3X.10 OTM val predictions)
- `artifacts/calibration/3X11_anp_otm.json` (gitignored)
- `tests/test_calibration_anp_otm.py`
- BOARD / PHASE3_INDEX / progress log / experiment journal

## Non-goals

- Do NOT change the calibration recipe vs 2D.4 / 3B.6 (faithful
  restatement; only the input predictions change).
- Do NOT retune the decision-layer thresholds (Q2 — that is held
  constant; not this story and not 3X).
- Do NOT use dirty predictions as input.

## Compute requirements

- **Locale:** local Mac
- **Hardware class:** CPU-only
- **Approx. VRAM:** n/a
- **Approx. wall time:** ~5–10 min
- **Disk — local committed artifacts:** config + test + fit-manifest
  (<200 KB); calibrator JSON gitignored
- **Disk — remote working set:** none
- **Notes:** requires the OTM val-prediction parquets from 3X.9 / 3X.10
  pulled back locally (small). If they were left Pod-side, regenerate
  the calibrator Pod-side as 3B.7 did — bit-identical.

## Implementation steps

1. Confirm 3X.9 + 3X.10 done; pull their OTM val predictions if needed.
2. Clone the 3B.6 calibration config; repoint inputs at the OTM
   predictions.
3. Run the calibration fit; record `T`, ensemble-scale, masking flag.
4. Write `tests/test_calibration_anp_otm.py` mirroring 3B.6's parity test.
5. Journal the OTM fit + the dirty-vs-OTM calibrator-parameter contrast.
6. PMR gate dry-run; flip BOARD to `in_review`.

## Tests to run / add

- [ ] `pytest tests/test_calibration_anp_otm.py -v`
- [ ] Fit converges; val coverage @0.90 within ±2 pp; hi-conf MAE <
      no-abstention MAE.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- `configs/calibration_3X11_anp_otm.yaml`
- `artifacts/calibration/3X11_anp_otm.json` (gitignored)
- `tests/test_calibration_anp_otm.py`; journal entry with the OTM fit
  parameters + dirty-vs-OTM contrast.

## Acceptance criteria

- [ ] Calibrator fit converges on OTM val predictions.
- [ ] OTM val coverage @0.90 within ±2 pp.
- [ ] OTM val hi-conf MAE < no-abstention MAE.
- [ ] Parity test green.
- [ ] PMR gate dry-run passes.

## Rollback notes

`git rm configs/calibration_3X11_anp_otm.yaml tests/test_calibration_anp_otm.py`;
`rm artifacts/calibration/3X11_anp_otm.json`. No data mutated.

---

## Last checkpoint

### 2026-05-29T23:55:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of 3X.1 decomposition.
- Last touched: this file.
- Next concrete action: 3X.9 + 3X.10 close; local session re-fits the
  calibrator on OTM val predictions.
- Open blocker: 3X.9 + 3X.10 must be `done`.

### 2026-06-02T00:30:00-04:00 — implemented; flipped to in_review

- Last completed: local CPU re-fit of the 2D.4 / 3B.6 fused calibrator
  on 3X.9 gaussian val (joined with 3X.10 K=5 ensemble disagreement).
- Acceptance gate met: val coverage @0.90 = **0.9000** (within ±2 pp);
  val hi-conf MAE 0.00849 < val no-abstention MAE 0.01349; parity test
  `tests/test_calibration_anp_otm.py` 6/6 green; PMR gate dry-run
  pending below.
- Fit params: `T=1.0050`, `ensemble_scale=1.9090`, `u0=0.01769`,
  `u_scale=0.006445`, `n_fit=2,638,892`.
- Notable observation (not a blocker): test fold coverage drifts to
  0.8656 (–3.4 pp), versus 3B.6 dirty test 0.8901; err/u rank
  correlation also drops (pearson 0.61, spearman 0.40 vs 3B.6's
  0.78 / 0.81). Flagged for 3X.13 dirty-vs-OTM tables.
- Files committed: `configs/calibration_3X11_anp_otm.yaml`,
  `tests/test_calibration_anp_otm.py`. Fit-manifest summary is
  inline in `docs/logs/progress_log.md` + `docs/experiments/
  experiment_journal.md` (3B.6 precedent — the whole
  `artifacts/calibration/` tree is gitignored).
- Gitignored (regenerable from config + cached CSVs):
  `artifacts/calibration/3X11_anp_otm.json` and
  `artifacts/calibration/3X11_anp_otm.report.json`.
- Next concrete action: operator reviews → `done`; 3X.12 (remote
  decision-layer eval, Q2) unblocks next.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

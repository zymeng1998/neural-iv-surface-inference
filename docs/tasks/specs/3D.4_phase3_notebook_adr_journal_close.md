# Story 3D.4 (Local): Phase 3 Notebook + ADR 0009 Finalize + Journal Close-out

---
id: 3D.4
epic: 3D
status: backlog   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-06-15T00:00:00-04:00
last_updated_at: 2026-06-15T00:00:00-04:00
parallel_safe_with: []
file_scope:
  - "notebooks/06_phase3_results.ipynb"
  - "docs/decisions/0009_phase3_production_predictor_selection.md"
  - "docs/experiments/experiment_journal.md"
  - "docs/roadmaps/phase3_accuracy_push.md"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3D.4_phase3_notebook_adr_journal_close.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "README.md"
  - "STATUS.md"
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`. Status changes happen in place; this file is
> never moved or deleted.
>
> `file_scope` is a **hard contract**.

## Purpose

Close epic 3D (and Phase 3). Run the 3D.2 generator to emit the committed
`notebooks/06_phase3_results.ipynb`; finalise
[ADR 0009](../../decisions/0009_phase3_production_predictor_selection.md)
(flip Proposed → Accepted/Implemented, fill the Outcome block with the
3D.3 memo verdict); write the Phase 3 closing entry in the experiment
journal; flip epic 3D → `done`; and — if the memo warrants it — create a
**Phase 4 epic placeholder** (`backlog`) on the BOARD. **Do not decompose
Phase 4 here.**

## Where this runs

**Local (Mac), CPU-only.** Runs the committed generator + writes docs.
No model load, no Pod, no GPU.

## Scope contract (atomic story)

- **One question:** is Phase 3 closeable — notebook regenerated, ADR 0009
  verdict locked, journal closed — with the production recommendation and
  Phase 4 direction recorded?
- **One artifact bundle:** the committed `06_phase3_results.ipynb` + the
  finalised ADR 0009 Outcome + the journal close-out entry.
- **One acceptance check:** notebook executes top-to-bottom with 0 cell
  errors; ADR 0009 status = Accepted/Implemented with a filled Outcome;
  epic 3D = `done`.

## Dependencies

- 3D.2 `done` (the generator script exists and is green).
- 3D.3 `done` (the memo verdict the ADR Outcome cites).

## Context files to read first

- `scripts/generate_phase3_results_notebook.py` (3D.2 output).
- `docs/phase3_result_memo.md` (3D.3 output — the verdict source).
- `docs/decisions/0009_phase3_production_predictor_selection.md` (skeleton).
- `docs/experiments/experiment_journal.md` (3X.14 / 3C.8 close-out entries
  as templates).
- `docs/tasks/specs/2D.10_phase2_results_memo_notebook.md` (close precedent).

## Files likely to touch

- `notebooks/06_phase3_results.ipynb` (new — generated, committed).
- `docs/decisions/0009_phase3_production_predictor_selection.md` (finalise).
- `docs/experiments/experiment_journal.md` (Phase 3 close-out entry).
- `docs/roadmaps/phase3_accuracy_push.md` (final §W13 / status block flip).
- `README.md` (flip Phase 3 → closed; record Phase 4 direction).
- `docs/tasks/BOARD.md` (epic 3D → `done`; optional Phase 4 placeholder row).
- `STATUS.md`, `docs/PHASE3_INDEX.md`, `docs/logs/progress_log.md`.

## Non-goals

- Do NOT re-run any model / eval / training; the notebook references
  committed artifacts only.
- Do NOT decompose Phase 4 (a `backlog` epic placeholder row is the most
  this story may create).
- Do NOT edit the generator logic (that is 3D.2); if it needs a fix, stop
  and route back to 3D.2.
- Do NOT alter the 3D.3 memo numbers; the ADR Outcome cites them as-is.

## Implementation steps

1. `python3 scripts/generate_phase3_results_notebook.py`; verify it exits
   0 and the notebook executes top-to-bottom with 0 cell errors.
2. Finalise ADR 0009: flip status, fill the Outcome block from the memo,
   expand Alternatives if needed.
3. Write the Phase 3 close-out journal entry (headline ladder + verdict).
4. Flip epic 3D → `done`; flip 3D.2 / 3D.3 / 3D.4 → `done`; optionally add
   a Phase 4 epic placeholder (`backlog`).
5. Update roadmap §W13 status block, README Phase 3 callout, PHASE3_INDEX,
   STATUS, progress_log.
6. Run the PMR **live** gate (notebook + journal are evidence-class).

## Tests to run / add

- [ ] `jupyter nbconvert --to notebook --execute
      notebooks/06_phase3_results.ipynb --output /tmp/_3D4_check.ipynb`
      runs top-to-bottom with 0 cell errors.
- [ ] Every artifact path cited in the notebook + ADR Outcome exists.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose` (live).

## Expected artifacts

- `notebooks/06_phase3_results.ipynb` (committed).
- Finalised ADR 0009.
- Experiment-journal close-out entry.
- Epic 3D `done`; doc sync.

## Acceptance criteria

- [ ] Notebook executes with 0 cell errors and renders the ladder, the
      dirty-vs-clean restatement, the per-epic figure pages, training
      curves, and the acceptance-criteria map.
- [ ] ADR 0009 = Accepted/Implemented with a filled Outcome block citing
      the memo.
- [ ] Journal close-out entry committed; epic 3D = `done`.
- [ ] Every numeric claim traces to a committed artifact path.
- [ ] PMR live gate passes.

## Rollback notes

`git checkout -- notebooks/06_phase3_results.ipynb
docs/decisions/0009_phase3_production_predictor_selection.md
docs/experiments/experiment_journal.md` (+ doc edits). No model / data /
predictor artifact mutated.

---

## Last checkpoint

### 2026-06-15T00:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of the 3D.1 decomposition.
- Last touched: this file.
- Next concrete action: gate on 3D.2 + 3D.3 close, then run the generator
  and finalise the ADR + journal.
- Open blocker: 3D.2, 3D.3.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

# Story 2A.4: Abstention / selective-prediction curves

---
id: 2A.4
epic: 2A
status: done   # backlog | todo | in_progress | in_review | blocked | done
created_at: 2026-05-22T01:29:47-04:00
last_updated_at: 2026-05-22T09:20:00-04:00
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`. Status changes happen in place; this file is never moved
> or deleted.

## Purpose

Implement the abstention / selective-prediction layer of W1: the risk–coverage
curve (error vs retained fraction as the abstention threshold sweeps) and
high-confidence MAE on the retained subset. Roadmap W1 names "abstention curves
(error vs coverage as the abstention threshold varies)" and "high-confidence MAE
(error on the retained, non-abstained subset)" as required metrics. This is the
measurement that later lets W5 justify an `abstain_flag`. Split from 2A.3
because it produces a *curve* (a sweep) rather than scalar metrics, and has a
distinct artifact shape.

## Context files to read first

- `docs/roadmaps/phase2_reliability_aware_surface_inference.md` (W1 bullets, W5 abstention)
- `src/neural_iv_surface_inference/eval/uncertainty_metrics.py` (high_confidence_mae from 2A.3)

## Files likely to touch

- `src/neural_iv_surface_inference/eval/abstention.py` (new)
- `tests/test_abstention.py` (new)

## Non-goals

- Do NOT pick or hard-code an abstention threshold / policy — that is W5 (2D).
- Do NOT build the end-to-end runner or write CSV/figure artifacts to disk —
  the runner (2A.5) owns persistence; this story returns curve data structures.
- Do NOT consume real model uncertainty; operate on supplied arrays.

## Implementation steps

1. Implement `risk_coverage_curve(abs_error, confidence, n_points) ->`
   arrays of `(coverage_fraction, retained_mae)` as the abstention threshold
   sweeps from "keep all" to "keep most-confident only". Return as a typed
   structure (dataclass or DataFrame).
2. Implement an aggregate scalar: `area_under_risk_coverage` (lower is better)
   for compact model comparison.
3. Confirm the curve's "keep-all" endpoint equals overall MAE and is monotone
   non-increasing under perfectly-ranked confidence (sanity invariant).
4. Unit-test with synthetic data where confidence perfectly ranks error
   (curve must be monotone decreasing) and where confidence is random (curve
   ≈ flat at overall MAE).

## Tests to run / add

- [ ] `pytest tests/test_abstention.py -q`
- [ ] `python3 scripts/smoke_test.py`

## Expected artifacts

- New module `src/neural_iv_surface_inference/eval/abstention.py`.
- New test file `tests/test_abstention.py`.

## Acceptance criteria

- [ ] `risk_coverage_curve` returns aligned coverage/error arrays; keep-all
      endpoint equals overall MAE.
- [ ] `area_under_risk_coverage` implemented and tested.
- [ ] Perfect-ranking synthetic case yields monotone-decreasing retained error;
      random-confidence case stays ≈ flat (within tolerance).
- [ ] New tests pass; smoke test still passes.

## Rollback notes

Purely additive module + test. To undo: delete `eval/abstention.py` and
`tests/test_abstention.py`. No existing code or data mutated.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal: Implement abstention / risk–coverage curves (2A.4).
- Mode: Implement (user authorized promote + start).
- Files inspected: eval/uncertainty_metrics.py (2A.3 conventions / high_confidence_mae),
  roadmap W1+W5 bullets.
- Files changed: src/neural_iv_surface_inference/eval/abstention.py (new),
  tests/test_abstention.py (new).
- Tests run: pytest tests/test_abstention.py (14 passed); full suite tests/
  (109 passed); scripts/smoke_test.py (exit 0).
- Results: RiskCoverageCurve frozen dataclass (coverage, retained_mae,
  n_retained, length-validated); risk_coverage_curve() sweeps retain-count
  k=1..n via cumulative mean of confidence-ranked error (keep-all endpoint ==
  overall MAE, coverage[-1]==1.0); area_under_risk_coverage() trapezoidal AURC
  (lower better). Perfect ranking → monotone non-decreasing retained error with
  coverage; random confidence → ≈ flat at overall MAE; AURC(perfect) < AURC(random).
- Open questions: none. AURC integrates over realized coverage support
  [k_min/n, 1]; full-[0,1] normalization deferred unless a comparison needs it.
- Risks: np.trapezoid (NumPy>=2) vs np.trapz (NumPy 1.26 here) handled via
  getattr fallback.
- Recommended next task: 2A.5 (uncertainty-evaluation runner + artifacts).
- Exact prompt for next Claude Code session: "Implement mode. Story 2A.5. Read
  docs/tasks/specs/2A.5_evaluation_runner_artifacts.md and only the context
  files it lists. Restate scope and acceptance criteria, propose a step plan,
  and wait for approval before editing."
```

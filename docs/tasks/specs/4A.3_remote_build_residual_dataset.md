# Story 4A.3 (Remote, CPU): Build the Full Residual-Target Dataset on OTM

---
id: 4A.3
epic: 4A
status: in_review   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-06-18T00:00:00-04:00
last_updated_at: 2026-06-19T00:00:00-04:00
parallel_safe_with: []
file_scope:
  - "scripts/build_residual_targets.py"
  - "artifacts/runs/4A3/**"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/4A.3_remote_build_residual_dataset.md"
  - "docs/PHASE4_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
  - "docs/data/data_lineage.md"
  - "STATUS.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a hard
> contract.

## Purpose

Materialise the full residual-target dataset on
`spy_phase1_random40_noiselow_otm`: run the per-date RBF baseline at every
query coordinate (train/val/test) via the 4A.2 builder and store
`r = iv_true − rbf_pred` so 4A.4 can train `f_θ` on residuals without
recomputing RBF each epoch. CPU-heavy (RBF over the full OTM fold), so it is
its own remote story, mirroring 3X.6's RBF-on-OTM run.

## Where this runs

**Remote (RunPod) CPU pod** (no GPU). The 3X.6 RBF-on-OTM run is the cost
analogue (~minutes per split on CPU).

## Scope contract (atomic story)

- **One question:** what are the residual targets `r = iv − rbf_pred` across
  the full OTM train/val/test, and are they finite and consistent with the
  3X.6 RBF MAE (`rbf_pred` mean abs ≈ the RBF residual)?
- **One artifact bundle:** the committed residual-target manifest + summary
  under `artifacts/runs/4A3/` (residual parquet itself gitignored, pulled to
  local for 4A.4); per-split row counts + finiteness + residual stats.
- **One acceptance check:** residual row count == query row count per split;
  0 non-finite; `mean(|r|)` on each split matches the 3X.6 RBF MAE within
  tolerance (sanity that `rbf_pred` is the same baseline).

## Dependencies

- 4A.2 `done` (builder + flag committed and on the Pod's `main`).
- `random40_noiselow_otm` on the Pod volume (already present from 3X).

## Context files to read first

- `docs/tasks/specs/4A.2_residual_target_builder.md`
- `docs/tasks/specs/3X.6_remote_rbf_on_otm_baseline.md` (the RBF run this
  mirrors; expected `mean(|r|)` ≈ 0.00613 test / 0.00615 val).

## Files likely to touch

- `scripts/build_residual_targets.py` (driver invocation; any pod-side
  parallelism tweak).
- `artifacts/runs/4A3/manifest.json` (+ `residual_stats.csv`) committed;
  `residual_{train,val,test}.parquet` gitignored, pulled to local.

## Non-goals

- Do NOT train (4A.4). Do NOT change the RBF math or the builder logic
  (4A.2). Do NOT rebuild the OTM benchmark.

## Implementation steps

1. Sync `main` on the pod; confirm 4A.2 landed.
2. Run `build_residual_targets.py` for the OTM dataset, all splits, `tee` to
   `runs/4A3/build.log`.
3. Verify row counts, finiteness, and `mean(|r|)` ≈ 3X.6 RBF MAE per split.
4. Pull manifest + stats to local (residual parquet to local for 4A.4).
5. Journal entry with residual stats; PMR gate dry-run.

## Tests to run / add

- [ ] Residual rows == query rows per split; 0 non-finite.
- [ ] `mean(|r|)` per split within tolerance of the 3X.6 RBF MAE.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose`

## Expected artifacts

- `artifacts/runs/4A3/manifest.json` + `residual_stats.csv` (committed);
  residual parquet (gitignored, pulled local); journal entry.

## Acceptance criteria

- [ ] Full residual dataset built; finiteness + row-count + MAE-sanity pass.
- [ ] Journal entry committed; PMR gate passes.

## Compute requirements

- **Locale:** remote (RunPod)
- **Hardware class:** CPU pod (no GPU)
- **Approx. VRAM:** n/a
- **Approx. wall time:** ~1–2 h (per-date RBF over the full OTM fold; cost
  analogue of 3X.6)
- **Disk — local committed artifacts:** ~1 MB (manifest + stats)
- **Disk — remote working set:** ~3–6 GB (residual parquet over all splits)
- **Notes:** can run on the same pod before the 4A.4 GPU window to amortise
  rental; OTM benchmark already on the volume from 3X — do not re-download.

## Rollback notes

Pod: `rm -rf runs/4A3`. Local: `git rm -r artifacts/runs/4A3/`.

---

## Last checkpoint

### 2026-06-18T00:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of the 4A.1 decomposition.
- Next concrete action: gate on 4A.2 close; then build on a CPU pod.
- Open blocker: 4A.2.

### 2026-06-19T00:00:00-04:00 — executed on RunPod CPU pod (`in_review`)

- Ran the full residual build on `random40_noiselow_otm` (4622 dates,
  10,531,499 rows). Result: **0 non-finite** `rbf_pred`/`residual_target`;
  per-split mean|residual| **train 0.006659 / val 0.006151 / test
  0.006132**. The val/test values **match the 3X.6 RBF MAE byte-for-byte**
  (0.006151 / 0.006132), confirming `rbf_pred` is the same RBF baseline →
  the residual targets are correct.
- **Two pod frictions handled (operator pod):** (1) the pod could not
  `git fetch` (SSH `origin`, no authorized GitHub key) → the two 4A.2 files
  (`residual_targets.py`, `build_residual_targets.py`) were **scp'd** to the
  pod (the builder only needs `models/interpolation.py`, present at the pod's
  older checkout). (2) the container is **cgroup-capped at ~3.7 GB**, so the
  full 41-column read OOM-killed → added a memory-safe `--columns` subset to
  the builder (the 7 columns the residual build + `minimal`-feature 4A.4
  training need); peak RSS ~2.3 GB.
- Output `…_otm_residual.parquet` (237 MB, 8 cols) is on the **persistent
  `/workspace` volume** (gitignored; **not** pulled to local — avoids a
  237 MB round-trip; 4A.4 reads it from the volume, or pull on demand). The
  committed evidence is `artifacts/runs/4A3/manifest.json` +
  `residual_stats.csv` (pulled to local).
- **The CPU pod can be terminated now** — the parquet is on the persistent
  volume and the stats are local.
- Next concrete action: operator promotes 4A.3 → `done`; then 4A.4 (remote
  GPU — gated on Pod go-ahead) trains the residual hybrid.
- Open blocker: none (4A.4 needs a GPU pod that mounts the same volume).

---

## Handoff summary

```text
## Session Summary
- Goal: materialise the full OTM residual-target dataset for 4A.4.
- Mode: remote CPU (RunPod, venv-2e2, 2 cores, 3.7 GB cap).
- Files inspected: build_residual_targets.py, residual_targets.py, the OTM
  benchmark parquet schema, the 3X.6 RBF metrics_summary (reference).
- Files changed: scripts/build_residual_targets.py (--columns subset),
  artifacts/runs/4A3/{manifest.json,residual_stats.csv}, 4A.3 spec, BOARD,
  PHASE4_INDEX, progress_log, experiment_journal, data_lineage, STATUS.
- Tests run: per-split finiteness + mean|residual| audit (val/test ==
  3X.6 RBF MAE exactly); local builder --columns smoke (exit 0).
- Results: residual parquet on /workspace (237 MB); 0 non-finite; targets
  validated against the RBF floor.
- Open questions: does the 4A.4 GPU pod mount the same /workspace volume?
  (If not, pull the parquet to local and re-upload.)
- Risks: none — residual = iv_clean − rbf_pred, RBF reused verbatim.
- Recommended next task: 4A.4 (remote GPU residual-hybrid training) — gated
  on operator Pod go-ahead.
- Exact prompt for next Claude Code session: "Run 4A.4: train the residual
  hybrid (ANP-residual default, target_mode=residual) on the OTM residual
  parquet across 3 heads; report summed σ̂ MAE vs RBF 0.00613 + 3X.9."
```

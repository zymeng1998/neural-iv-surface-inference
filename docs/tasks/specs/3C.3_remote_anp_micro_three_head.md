# Story 3C.3 (Remote, GPU): ANP + `micro_v1` Three-Head Retrain on OTM

---
id: 3C.3
epic: 3C
status: todo
created_at: 2026-06-02T22:00:00-04:00
last_updated_at: 2026-06-02T22:00:00-04:00
parallel_safe_with: ["3D.1"]
file_scope:
  - "configs/conditional_3C3_anp_micro_gaussian_otm.yaml"
  - "configs/conditional_3C3_anp_micro_quantile_otm.yaml"
  - "configs/conditional_3C3_anp_micro_point_otm.yaml"
  - "scripts/run_3c3_anp_micro.py"
  - "scripts/run_3c3_anp_micro.sh"
  - "artifacts/runs/3C3/**"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/3C.3_remote_anp_micro_three_head.md"
  - "docs/PHASE3_INDEX.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a
> hard contract.

## Purpose

Retrain the end-to-end **DeepSets+ANP** conditional model on
`spy_phase1_random40_noiselow_otm` with `feature_set: micro_v1` (ADR
0008) across **all three head kinds** (gaussian / quantile / point).
Faithful head-by-head restatement of 3X.9 with the only change being
the per-quote feature tuple — same dataset, same seed, same optimizer
schedule, same `decoder_kind: anp`, same `coord_encoding.kind: raw`.
This is the headline 3C model and the input to the calibration / eval
chain (3C.5 / 3C.6).

## Where this runs

**Remote (RunPod), GPU pod.** Sequential trainings mirroring 3X.9.
Checkpoints Pod-side; manifests + curves + per-row val/test predictions
pulled back to local for 3C.5+.

## Scope contract (atomic story)

- **One question:** what is the ANP+`micro_v1` test MAE, val/test NLL,
  and quantile coverage/monotonicity on
  `spy_phase1_random40_noiselow_otm` across all three heads, and how
  do those numbers compare to the matched 3X.9 OTM baselines (test MAE
  gaussian 0.01440 / quantile 0.01175 / point 0.00987) and the
  RBF-on-OTM floor (test MAE 0.00613)?
- **One artifact bundle:** `artifacts/runs/3C3/{gaussian, quantile,
  point}/` mirroring the 3X.9 bundle schema.
- **One acceptance check:** all three complete with finite losses;
  quantile monotonic on test; manifests carry `feature_set=micro_v1`
  + per-feature z-score stats + dataset/config/seed/code hashes;
  ANP-vs-RBF and ANP-vs-ANP-baseline deltas journaled per head.

## Dependencies

- 3C.2 `done` (the loader / encoder / model / predictor flag must be
  landed and committed).
- ADR 0008 `Accepted` and on `main`.
- `random40_noiselow_otm` on the Pod volume (already present from 3X
  — do not rebuild).
- 3X.9 / 3X.10 / 3X.11 / 3X.12 bundles committed (the matched OTM
  baselines this story is benchmarked against).

## Context files to read first

- [ADR 0008](../../decisions/0008_microstructure_feature_set_freeze.md)
- `configs/conditional_3X9_anp_gaussian_otm.yaml`,
  `configs/conditional_3X9_anp_quantile_otm.yaml`,
  `configs/conditional_3X9_anp_point_control_otm.yaml`
- `scripts/run_3x9_anp.py`, `scripts/run_3x9_anp.sh` (the protocol
  this story mirrors)
- `docs/tasks/specs/3X.9_remote_anp_on_otm.md`
- `results/3/spy_phase1_random40_noiselow_otm/3x_compare/comparison.csv`
  (OTM baseline numbers per head)
- `artifacts/runs/3X9/{gaussian,quantile,point_control}/manifest.json`
  (the seed / hparam contract this story preserves)

## Files likely to touch

- `configs/conditional_3C3_anp_micro_{gaussian,quantile,point}_otm.yaml`
  — clones of the 3X.9 configs with the **only** delta
  `data.feature_set: micro_v1`. Same seed, same hparams, same
  `decoder_kind: anp`, same `coord_encoding.kind: raw`, same
  `freeze_encoder: false`, same epoch budget. New z-score stats are
  computed by `train_conditional` at run start on the OTM train split
  and persisted in the manifest (ADR 0008 §Normalization).
- `scripts/run_3c3_anp_micro.{py,sh}` — clones of the 3X.9 driver with
  the three new config paths.
- `artifacts/runs/3C3/{gaussian,quantile,point}/` — committed
  `manifest.json` per head (~30 kB) + `training_curve.csv`;
  `predictions_{val,test}.parquet` (~150 MB total) gitignored per the
  `artifacts/runs/**/*.parquet` convention but pulled to local for
  3C.5 / 3C.7.

## Non-goals

- Do NOT modify `ANPDecoder`, `SetEncoder`, `ConditionalSurfaceModel`,
  or any loader — every change is in 3C.2 (already shipped).
- Do NOT warm-start from any dirty or OTM `minimal`-feature checkpoint
  (the encoder's input-projection layer has a different `in_features`;
  weight loading would silently truncate). From-scratch only, mirrors
  3X.9's D8.
- Do NOT run on any benchmark other than `random40_noiselow_otm`.
- Do NOT change the decoder, the coordinate encoding, the head menu,
  or the seed / hparams.
- Do NOT train an ensemble here (that is 3C.4).
- Do NOT re-fit the calibrator here (that is 3C.5).
- Do NOT make Phase 3 bar pass/fail claims based on raw test MAE
  alone — the bar is adjudicated end-to-end via 3C.6.

## Compute requirements

- **Locale:** remote (RunPod)
- **Hardware class:** single GPU (3X.9 tier — RTX A4500 / 4090 / A40)
- **Approx. VRAM:** ~8–14 GB (ANP cross-attention + the 9-dim context
  adds a small parameter bump in `SetEncoder` input projection only)
- **Approx. wall time:** ~45–70 min per head, sequential → **~2.5–3.5 h
  total**
- **Disk — local committed artifacts:** ~30 MB per head (manifests +
  curves; predictions parquet gitignored)
- **Disk — remote working set:** 3 × ~300 MB checkpoints + ~6 GB
  intermediate (3 × val/test predictions) → **≥ 12 GB free beyond the
  OTM data**
- **Notes:** same Pod tier as 3X.9. Best amortised in the same Pod
  window as 3C.4 (ensemble) to avoid two rentals. OTM benchmark
  (~3 GB) should already be on the Pod volume from 3X — do not
  re-download. If pod was terminated after 3X.14, plan ~10 min to
  re-sync.

## Implementation steps

1. Confirm 3C.2 `done` and on the Pod's checkout of `main`.
2. Clone the three 3X.9 configs; set `data.feature_set: micro_v1`;
   leave all other fields untouched (operator should `diff` against
   the 3X.9 configs and confirm only the feature_set line changes).
3. Set up the GPU pod (or re-attach to the 3X-era pod if still warm).
4. Run gaussian → quantile → point sequentially, each logged with
   `tee` to `runs/3C3/<head>/train.log`.
5. Pull back manifests + curves + per-row val/test predictions to
   local.
6. Verify quantile monotonicity on test; verify finite losses; verify
   prediction row count matches benchmark query row count.
7. Append an entry to `docs/experiments/experiment_journal.md` with
   per-head test MAE, val MAE, NLL, ANP-vs-RBF gap, ANP-vs-3X.9 delta.
8. Run PMR gate dry-run.
9. Flip BOARD row → `in_review`.

## Tests to run / add

- [ ] All three trainings complete with finite losses.
- [ ] Quantile monotonic on test (`q_lo ≤ q_med ≤ q_hi`).
- [ ] Prediction rows == query rows.
- [ ] Manifests record `feature_set=micro_v1`, `decoder_kind=anp`,
      `coord_encoding.kind=raw`, dataset/config/seed/code hashes,
      and per-feature z-score stats fitted on the OTM train split.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- `artifacts/runs/3C3/{gaussian,quantile,point}/manifest.json` (committed).
- `artifacts/runs/3C3/{gaussian,quantile,point}/training_curve.csv`
  (committed).
- `artifacts/runs/3C3/{gaussian,quantile,point}/predictions_{val,test}.parquet`
  (gitignored; pulled to local for 3C.5 + 3C.7).
- `docs/experiments/experiment_journal.md` entry.

## Acceptance criteria

- [ ] All three trainings complete; validation loss decreases from
      early epochs and stabilizes.
- [ ] Quantile monotonicity holds on test.
- [ ] Manifests carry the ADR 0008 feature_set field + z-score stats.
- [ ] ANP-vs-3X.9 and ANP-vs-RBF deltas reported as diagnostics
      per head (pass/fail is adjudicated end-to-end at 3C.6).
- [ ] Journal entry committed.
- [ ] PMR gate dry-run passes.

## Rollback notes

Pod: `rm -rf runs/3C3`. Local: `git rm -r artifacts/runs/3C3/`;
`git rm configs/conditional_3C3_anp_micro_*_otm.yaml
scripts/run_3c3_anp_micro.*`.

---

## Last checkpoint

### 2026-06-02T22:00:00-04:00 — registered (not yet executed)

- Last completed: spec authored as part of 3C.1 decomposition.
- Last touched: this file.
- Next concrete action: gate on 3C.2 close.
- Open blocker: 3C.2.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal:
- Mode:
- Files inspected:
- Files changed:
- Tests run:
- Results:
- Open questions:
- Risks:
- Recommended next task:
- Exact prompt for next Claude Code session:
```

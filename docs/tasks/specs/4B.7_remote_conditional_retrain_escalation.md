# Story 4B.7 (Remote, GPU): CONDITIONAL Per-Regime Retrain Escalation (only if 4B.5 ambiguous)

---
id: 4B.7
epic: 4B
status: in_progress   # backlog | todo | in_progress | in_review | blocked | done | cancelled
created_at: 2026-06-21T00:00:00-04:00
last_updated_at: 2026-06-22T01:00:00-04:00

# REOPENED 2026-06-22: initially cancelled when 4B.5 returned `ambiguous` and the
# operator declined the escalation, then **reversed** — the operator chose to run
# the FULL fair retrain after all (all 4 regimes × 4 non-dense rungs = 16 cells)
# to resolve the eval-time OOD ambiguity definitively. The 4B.6 close + ADR 0011
# Outcome (accuracy retired) are correspondingly **reopened / provisional** until
# this story resolves the gate. Runs on a fresh GPU pod.
parallel_safe_with: []
file_scope:
  - "configs/conditional_4B7_retrain_*.yaml"
  - "scripts/run_4b7_retrain_sweep.py"
  - "scripts/run_4b7_retrain_sweep.sh"
  - "artifacts/runs/4B7/**"
  - "results/4/spy_phase1_random40_noiselow_otm/4b_sweep/**"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/4B.7_remote_conditional_retrain_escalation.md"
  - "docs/logs/progress_log.md"
  - "docs/experiments/experiment_journal.md"
  - "STATUS.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a hard
> contract.

## Purpose

**Conditional escalation** for the staged design. The 4B.4/4B.5 eval-time sweep
stresses a checkpoint trained on *full* context against *sparse* context, which
carries a fairness caveat. **Only if 4B.5 returns `ambiguous`** (trend present
but CIs do not separate, or the caveat materially clouds the read), this story
**retrains** the RBF-prior hybrid *per rung* on the contested regime(s) for an
apples-to-apples comparison, then recomputes that regime's trajectory and
finalizes the gate. **If 4B.5 is unambiguous (`true`/`false`), this story is set
`cancelled`** with a one-line rationale — no Pod spend.

## Where this runs

**Remote (RunPod), GPU.** Per-rung retraining = multiple GPU windows — the
heaviest 4B compute. Rent only when triggered.

## Dependencies

- 4B.5 `done` **and** its `gate_verdict.json` = `ambiguous` (the trigger). If
  the verdict is `true`/`false`, cancel this story instead of running it.
- 4A.3 `done` (residual-target build path, reused per rung) + 4A.4 `done`
  (the training recipe to mirror).

## Scope contract (atomic story)

- **One question:** under a *fair* per-rung retrain, does the contested
  regime's edge grow materially or not (resolving the ambiguity)?
- **One artifact bundle:** per-rung retrained checkpoints (Pod) + refreshed
  `4b_sweep/` trajectory rows for the contested regime + a resolved
  `gate_verdict.json`.
- **One acceptance check:** the contested regime's trajectory is recomputed
  from fairly-trained models and `accuracy_survives` flips to `true`/`false`.

## Context files to read first

- `docs/tasks/specs/4A.4_remote_train_residual_hybrid.md` +
  `scripts/run_4a4_residual.py` (the training recipe to mirror per rung).
- `docs/tasks/specs/4B.3_*` / `4B.4_*` / `4B.5_*` (the inputs + the ambiguity
  this resolves).

## Files likely to touch

- `configs/conditional_4B7_retrain_*.yaml`, `scripts/run_4b7_retrain_sweep.{py,sh}`
- `artifacts/runs/4B7/**`, `results/4/…/4b_sweep/**`, journal, progress_log,
  BOARD, STATUS

## Non-goals

- Do NOT run if 4B.5 is unambiguous — cancel instead.
- Do NOT change the architecture, residual formulation, or success bar (mirror
  4A.4 exactly, varying only the per-rung training context).
- Do NOT re-open the Phase 4 verdict or add a data source.
- Do NOT retrain regimes that 4B.5 already resolved — only the contested one(s).

## Compute requirements

- **Locale:** remote (RunPod)
- **Hardware class:** single GPU (e.g. A40 / A100-40GB; mirror 4A.4's tier)
- **Approx. VRAM:** ~16–40 GB (per 4A.4)
- **Approx. wall time:** ~N_rungs × the 4A.4 train time per contested regime
  (e.g. 3–5 rungs × ~30–60 min = ~2–5 h GPU); **only if triggered**
- **Disk — local committed artifacts:** ~1–2 MB (refreshed trajectory rows +
  manifests; checkpoints stay on the Pod, gitignored)
- **Disk — remote working set:** ~N_rungs × (checkpoint + intermediates),
  reusing the OTM + 4B.3 data already on the volume → **allocate ≥ 10 GB free**
- **Notes:** **conditional** — most likely `cancelled`. Budget a GPU window
  only after 4B.5 returns `ambiguous`. Train only the contested regime's rungs.

## Implementation steps

1. Gate on `4b_sweep/gate_verdict.json == ambiguous`; otherwise cancel.
2. Per contested rung: build the rung's residual targets (reuse 4A.3 path),
   retrain the hybrid (mirror 4A.4), forward + sum → σ̂.
3. Recompute that regime's trajectory + CIs (reuse 4B.5); update
   `gate_verdict.json` to a resolved `true`/`false`.
4. Append journal; update progress_log / STATUS; hand to 4B.6 for the close.

## Tests to run / add

- [ ] Trigger check: only runs when verdict == `ambiguous`.
- [ ] Per-rung retrained σ̂ finite; contested-regime trajectory recomputed.
- [ ] `gate_verdict.json` resolves to `true`/`false`.
- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`

## Expected artifacts

- Pod checkpoints (gitignored) + refreshed `4b_sweep/` rows +
  `artifacts/runs/4B7/` manifests + resolved verdict.

## Acceptance criteria

- [ ] Either cancelled (4B.5 unambiguous) **or** the contested regime is
      retrained and the gate resolves.
- [ ] Gates pass; journal updated.

## Rollback notes

Retrained checkpoints are Pod-side + regenerable; committed artifacts small.
No Phase 4 / source data mutated. Cancel path leaves no artifacts.

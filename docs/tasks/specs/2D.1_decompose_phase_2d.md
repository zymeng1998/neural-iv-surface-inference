# Story 2D.1: Decompose Phase 2D (Uncertainty-Aware Inference & Decision Layer)

---
id: 2D.1
epic: 2D
status: done   # backlog | todo | in_progress | in_review | blocked | done
created_at: 2026-05-24T00:00:00+00:00
last_updated_at: 2026-05-24T00:00:00+00:00
---

> Keep the `status:` field above in sync with this story's row on
> `docs/tasks/BOARD.md`. Status changes happen in place; this file is never moved
> or deleted.

## Purpose

Phase-entry decomposition story for epic 2D (Phase 2 Workstreams W4 — *Uncertainty-
Aware Neural Inference* + W5 — *Abstention and Tradability Decision Layer*). It
pulls epic 2D into active work, breaks it into atomic stories each sized for one
focused session, writes a spec per story, and registers them on the board at
`backlog`. Mandatory first story of every epic per the operating model §4
("phase-entry decomposition").

Epics 2A (W1 measurement), 2B (W2 diagnostics), and 2C (W3 conditional model)
are all `done`. The Phase 1 baselines and the conditional Set-Encoder+Decoder
have been re-run against the Alpha Vantage dataset (2C.8). What is still
missing is the *reliability output* that motivates the project: the conditional
model currently emits point predictions only — `PredictionResult.uncertainty`,
`.lower`, `.upper` are all `None`.

Epic 2D fills these slots and converts the resulting reliability signals into
**decision-grade outputs**: `confidence_score`, `uncertainty_band`,
`tradability_score`, `no_arb_risk_flags`, and `abstain_flag`. By the end of 2D,
the system can refuse to predict in unreliable regions and emit a calibrated
confidence on the regions it does serve.

W4 supplies the uncertainty signals; W5 turns them into decisions. The two
workstreams share an evaluator (the W1 layer from 2A) and a structural-risk
input (the W2 `no_arb_risk_flags` from 2B), so they decompose as one epic.

## Context files to read first

- `docs/workflows/ai_human_collaboration.md` (§3 modes, §4 board lifecycle)
- `docs/tasks/README.md`
- `docs/tasks/_template.md`
- `docs/roadmaps/phase2_reliability_aware_surface_inference.md` (W4 + W5 / §5)
- `docs/tasks/specs/2C.1_decompose_phase_2c.md` (decomposition precedent)
- `src/neural_iv_surface_inference/eval/predictor.py` (the `PredictionResult`
  contract 2D must fill — `uncertainty`, `lower`, `upper`)
- `src/neural_iv_surface_inference/eval/uncertainty_metrics.py` (W1 metrics that
  must accept the new signals)
- `src/neural_iv_surface_inference/eval/abstention.py` (W1 abstention curves)
- `src/neural_iv_surface_inference/diagnostics/risk_flags.py` (W2 risk-flag
  output that W5 consumes)
- `src/neural_iv_surface_inference/models/conditional_surface.py` (W3 model that
  W4 extends with an uncertainty head / ensemble)

## Files likely to touch

- `docs/tasks/BOARD.md`
- `docs/tasks/specs/2D.1_decompose_phase_2d.md` (this file)
- `docs/tasks/specs/2D.2_heteroscedastic_quantile_head.md` (local)
- `docs/tasks/specs/2D.3_deep_ensemble_disagreement.md` (local)
- `docs/tasks/specs/2D.4_calibrated_confidence_score.md` (local)
- `docs/tasks/specs/2D.5_abstention_tradability_decision_layer.md` (local)
- `docs/tasks/specs/2D.6_decision_layer_runner_artifacts.md` (local)
- `docs/tasks/specs/2D.7_remote_train_heteroscedastic_quantile.md` (remote)
- `docs/tasks/specs/2D.8_remote_train_deep_ensemble.md` (remote)
- `docs/tasks/specs/2D.9_remote_decision_layer_e2e.md` (remote)
- `docs/tasks/specs/2D.10_phase2_results_memo_notebook.md` (local; added
  2026-05-25 as the Phase 2 closing synthesis story paralleling
  `docs/phase1_result_memo.md` and `notebooks/04_phase2c_results.ipynb`)
- `docs/logs/progress_log.md`
- `docs/roadmaps/phase2_reliability_aware_surface_inference.md` (W4+W5 status
  note)

## Non-goals

- Do NOT decompose any later epic — progressive decomposition only.
- Do NOT implement any 2D.2–2D.6 story (no model changes, no training runs, no
  evaluation runs).
- Do NOT promote new stories to `todo` — the human reviews and promotes them.
- Do NOT renumber, delete, or re-status existing board rows for 2A/2B/2C.
- Do NOT change the W1 metric or W2 diagnostic implementations themselves; 2D
  *consumes* both. Any required additions (e.g. an interval-coverage stratified
  by abstention) are surfaced as scope inside the relevant 2D story, not as
  retroactive 2A/2B edits.
- Do NOT introduce any new external data dependency. 2D operates on the
  existing Alpha Vantage benchmark datasets used by 2C.8.

## Story breakdown (the decomposition)

Eight implementation stories under 2D.1, split cleanly into a **local
phase** (Mac) and a **remote phase** (RunPod), mirroring the 2C precedent.
Each story is either fully local or fully remote — no story straddles.

### Local stories (Mac)

| Story | Scope | W4/W5 element | Depends on |
|---|---|---|---|
| 2D.2 Heteroscedastic / quantile head — code + synthetic smoke | Add an uncertainty head to the W3 decoder behind a `head.kind ∈ {point, gaussian, quantile}` config switch. Gaussian NLL or pinball loss (masked). Default `point` preserves 2C bit-for-bit. Unit tests + three synthetic-smoke trains. **No AV data, no Pod.** | W4 — predictive intervals (code) | 2C |
| 2D.3 Deep ensemble adapter — manifest + dummy tests + 2-member synthetic smoke | `scripts/run_ensemble_train.py`, `EnsembleConditionalPredictor` adapter, `members.json` manifest schema, deterministic-mock unit tests, 2-member synthetic-smoke train. No architecture change. **No AV data, no Pod.** | W4 — ensemble disagreement (code) | 2C |
| 2D.4 Calibrated confidence score | Fuse 2D.7 interval + 2D.8 disagreement + 2B.2 masking-sensitivity into one `confidence_score` + `(lower, upper)`; fit the calibrator (temperature for Gaussian, split-conformal for quantile) on **cached val predictions pulled from the Pod**; verify with the W1 coverage metric. Fills `PredictionResult.uncertainty/lower/upper`. **Local CPU.** | W4 — calibrated `confidence_score` | 2D.7, 2D.8 |
| 2D.5 Abstention + tradability + risk-flag decision layer | New `eval/decision_layer.py` consuming `(sigma_hat, confidence, lower, upper, no_arb_risk_flags)` and emitting `abstain_flag`, `tradability_score`, `decision_reason`. Config-driven thresholds (`configs/decision_layer.yaml`). Pure CPU NumPy + unit tests. | W5 — decision layer | 2D.4, 2B.4 |
| 2D.6 Decision-layer runner skeleton + synthetic smoke | New `scripts/run_decision_layer_eval.py` + `configs/decision_layer_eval.yaml` + report-table helpers. Synthetic smoke against a tiny synthetic dataset + dummy predictor asserting all expected files appear under a temp `results-root`. **No AV data, no committed `results/2D/` artifacts.** | W5 — runner skeleton | 2D.5 |
| 2D.10 Phase 2 results memo + notebook | New `docs/phase2_result_memo.md` + `notebooks/05_phase2_results.ipynb` + `scripts/generate_phase2_results_notebook.py`. Synthesis only: TL;DR, headline table from `results/2D/comparison_summary.csv`, vs-Phase-1 / vs-Phase-2C comparison, acceptance-criteria map §1–§7, open questions, worked single-date example showing all six decision-grade outputs as heatmaps. **No new training, no new evaluation runs.** | W4+W5 — Phase 2 closing narrative | 2D.9 |

### Remote stories (RunPod)

| Story | Scope | W4/W5 element | Depends on |
|---|---|---|---|
| 2D.7 Full AV training — Gaussian + quantile heads (+ point control) | Three AV trainings using 2D.2 code: `head.kind ∈ {gaussian, quantile, point-control}`. Pull back `manifest.json`, `training_curve.csv`, `val_predictions.csv`, `test_predictions.csv` per head. Point-control parity check vs 2C.5-R. Journal entry. **Pod GPU.** | W4 — interval signal (real data) | 2D.2 |
| 2D.8 K-seed deep ensemble training on AV | K = 5 full conditional trainings using 2D.3 code; score via the `EnsembleConditionalPredictor`; pull back manifest + per-row ensemble val/test predictions (mean + disagreement std + per-member columns) + per-member training curves. Journal entry. **Pod GPU.** | W4 — disagreement signal (real data) | 2D.3 |
| 2D.9 End-to-end decision-layer eval on AV + artifacts + journal | Run 2D.6's runner on the real AV benchmark with all four predictors (interpolation, MLP, point conditional, calibrated conditional from 2D.4). Commit `results/2D/<dataset>/<predictor>/...` + `results/2D/comparison_summary.csv`. Closing `experiment_journal.md` entry that satisfies acceptance §5 + §6. Bump reviewer-state. Live PMR gate. | W5 — closing evidence | 2D.4, 2D.5, 2D.6, 2D.7, 2D.8 |

### Phase sequencing

Sequencing principle: **all local work ships first**, then the operator
starts the Pod for the remote chain.

1. **Local phase A (parallel)** — 2D.2 and 2D.3 are independent code +
   synthetic-smoke stories; can run in either order or in parallel.
2. **Local phase B (parallel)** — 2D.5 (pure decision-layer code) and 2D.6
   (runner skeleton) need only 2D.5's own code path and a dummy predictor,
   so they can ship before any Pod work.
3. **Remote phase A (parallel)** — 2D.7 and 2D.8 are independent training
   passes (different heads vs ensemble) and can be queued in any order
   on the Pod.
4. **Local phase C** — 2D.4 fits the calibrator on the val predictions
   pulled back from 2D.7 + 2D.8.
5. **Remote phase B** — 2D.9 scores all four predictors end-to-end on AV
   using the calibrator from 2D.4 and the runner from 2D.6, then artifacts
   are pulled back to the laptop, committed, and journalled.
6. **Local phase D (closing synthesis)** — 2D.10 reads the committed
   `results/2D/` artifacts and the 2D.7 / 2D.8 training summaries and
   produces the Phase 2 results memo (`docs/phase2_result_memo.md`) plus
   the interactive results notebook (`notebooks/05_phase2_results.ipynb`).
   Pure narrative + reproducible notebook generation; no new runs.

## Implementation steps

1. Set epic 2D row to `in_progress` on `docs/tasks/BOARD.md`.
2. Write specs 2D.2–2D.10 from `_template.md`, fully filled. Stories
   2D.2–2D.6 are local-only; stories 2D.7–2D.9 are remote-only.
3. Add board rows for 2D.1–2D.10 (2D.1 `done` after this story commits;
   2D.2–2D.10 `backlog`).
4. Update the phase-2 roadmap status note (W4+W5 decomposed, 8-story
   local/remote split). Append a `docs/logs/progress_log.md` entry.
5. Run `python3 scripts/pmr_prepush_gate.py --verbose --dry-run`.
6. Mark this story `done` and hand off for human review.

## Tests to run / add

- [ ] `python3 scripts/pmr_prepush_gate.py --verbose --dry-run` (docs-only gate)

(No `pytest` / smoke test — this is a docs-only planning story.)

## Expected artifacts

- 10 story specs in `docs/tasks/specs/` (2D.1–2D.10).
- Updated `docs/tasks/BOARD.md` (epic 2D `in_progress` + 10 rows).
- Progress-log entry and roadmap W4+W5 status note.

## Acceptance criteria

- [ ] Epic 2D is `in_progress` on the board.
- [ ] Stories 2D.2–2D.10 each have a complete spec (all template sections filled)
      and a board row at `backlog`.
- [ ] Stories 2D.2–2D.6 are explicitly local-only; stories 2D.7–2D.9 are
      explicitly remote-only — no story straddles the local/remote
      boundary.
- [ ] 2D.1 row is `done`.
- [ ] PMR gate dry-run passes.
- [ ] No non-doc files changed; no other epic decomposed.

## Rollback notes

Docs-only change. To undo: `git checkout -- docs/tasks/ docs/logs/progress_log.md
docs/roadmaps/phase2_reliability_aware_surface_inference.md`. No data or code
mutated.

---

## Handoff summary (fill on completion)

```text
## Session Summary
- Goal: Enter epic 2D and decompose W4+W5 (uncertainty-aware inference + decision
  layer) into story specs.
- Mode: Plan / decomposition.
- Files inspected: phase2 roadmap W4+W5/§5, BOARD.md, _template.md, 2C.1
  decomposition precedent, eval/predictor.py (PredictionResult contract),
  eval/uncertainty_metrics.py, eval/abstention.py, diagnostics/risk_flags.py,
  models/conditional_surface.py.
- Files changed: BOARD.md (2D in_progress + 6 rows); specs/2D.1–2D.6;
  progress_log.md; phase2 roadmap W4+W5 status note.
- Tests run: pmr_prepush_gate.py --verbose --dry-run.
- Results: 6 story specs written; board updated; gate passes.
- Open questions: (1) 2D.2 — heteroscedastic Gaussian head vs quantile head: which
  do we ship first? Recommend Gaussian for simpler calibration story; quantile as
  fallback if Gaussian intervals are poorly calibrated. (2) 2D.3 — ensemble size
  K: recommend 5 (cheap + standard in the deep-ensembles literature); confirm.
  (3) 2D.4 — which masking-sensitivity signal from 2B.2 feeds the
  confidence_score? Recommend per-point prediction std under repeated masks at a
  fixed mask rate; confirm.
- Risks: 2D.4 is the heaviest story (signal fusion + calibration on held-out
  fold + W1 verification). If it bloats, split into 2D.4a (interval calibration)
  and 2D.4b (confidence-score fusion).
- Recommended next task: Human reviews 2D.2–2D.6, promotes ready ones to todo,
  then run 2D.2 or 2D.3 in Implement mode (they are independent).
- Exact prompt for next Claude Code session:
  "Implement mode. Story 2D.2. Read
   docs/tasks/specs/2D.2_heteroscedastic_quantile_head.md and only the context
   files it lists. Restate scope and acceptance criteria, propose a step plan,
   and wait for approval before editing."
```

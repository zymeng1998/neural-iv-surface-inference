# Story M1.1 (Local): Decompose Epic M1 + ADR 0007

---
id: M1.1
epic: M1
status: done
created_at: 2026-05-30T13:00:00-04:00
last_updated_at: 2026-05-30T14:05:00-04:00
parallel_safe_with: ["3X.*", "3B.*", "3C.*", "3D.*"]
file_scope:
  - "docs/decisions/0007_multi_agent_handoff.md"
  - "docs/roadmaps/meta1_agent_collaboration.md"
  - "docs/tasks/BOARD.md"
  - "docs/tasks/specs/M1.1_decompose_m1.md"
  - "docs/tasks/specs/M1.2_agents_md_and_cursor_bootstrap.md"
  - "docs/tasks/specs/M1.3_check_story_dependencies.md"
  - "docs/tasks/specs/M1.4_check_file_scope.md"
  - "docs/tasks/specs/M1.5_commit_msg_trailer_hook.md"
  - "docs/logs/progress_log.md"
---

> Keep `status:` in sync with `docs/tasks/BOARD.md`. `file_scope` is a
> hard contract.

## Purpose

Register epic M1 (multi-agent collaboration infrastructure) on the
BOARD, write [ADR 0007](../../decisions/0007_multi_agent_handoff.md)
explaining the discovery-vs-enforcement split, and lay down the four
implementation sub-specs (M1.2–M1.5) so they can be executed
atomically.

## Scope contract (atomic story)

- **One question:** is the M1 epic well-formed (ADR + roadmap + 4
  atomic sub-stories on the BOARD)?
- **One artifact bundle:** ADR 0007, roadmap, this spec, M1.2–M1.5
  specs, BOARD rows, progress log entry.
- **One acceptance check:** BOARD shows epic M1 + 5 rows; ADR 0007
  and all four sub-specs exist and parse; nothing under `src/`,
  `data_processed/`, or any research-phase file is touched.

## Dependencies

- None. This is a decomposition story for a new meta-epic.

## Context files to read first

- The 3X.4 review thread that motivated this (see
  [`docs/logs/progress_log.md`](../../logs/progress_log.md) latest
  entry once written).
- [`docs/tasks/specs/3X.1_decompose_phase_3x.md`](3X.1_decompose_phase_3x.md)
  as the precedent for decomposition-story format.

## Files likely to touch

See `file_scope`.

## Non-goals

- Writing any of the implementation code (M1.2–M1.5 own that).
- Promoting any rule to `~/.claude/`.

## Compute requirements

- **Locale:** local
- **Hardware class:** none
- **Approx. wall time:** ~30 min of authoring
- **Disk:** ~30 KB committed prose

## Implementation steps

1. Draft ADR 0007.
2. Draft roadmap `meta1_agent_collaboration.md`.
3. Draft this spec + M1.2 + M1.3 + M1.4 + M1.5 specs.
4. Add the M1 epic + 5 story rows to `docs/tasks/BOARD.md`.
5. Append a progress-log entry summarising the epic registration.
6. Verify the PMR gate still passes (`--dry-run`).

## Acceptance criteria

- [ ] ADR 0007 exists and is `Accepted`.
- [ ] Roadmap `meta1_agent_collaboration.md` exists with subtask matrix.
- [ ] BOARD has epic M1 + stories M1.1–M1.5.
- [ ] All 5 M1 spec files exist with parseable front-matter
      (`id`, `epic`, `status`, `file_scope`, `parallel_safe_with`).
- [ ] PMR gate dry-run passes.

## Rollback notes

`git rm` the new files; revert BOARD + progress-log edits. No tracked
runtime code touched.
